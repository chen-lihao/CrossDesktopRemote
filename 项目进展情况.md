# CrossDesktopRemote 项目进展情况

> 最后更新：2026-08-27
> 当前阶段：M1 Apple 稳定性验收 + M1B Windows 双向原型 + 文件/剪贴板阶段 2
> 当前状态：iPad→Mac 基本连接、画面和远程输入已验证。Mac/Windows 双向远控原型和桌面 DNS-SD 已接入。文件/剪贴板阶段 1 的协议、Rust 核心、C ABI 和 WebRTC 传输抽象已完成；阶段 2 已完成 macOS/Windows UTF-8 文本剪贴板、双向/单向/关闭策略、revision/SHA-256 回环抑制和设置 UI。iPad 不声明该能力，继续使用既有通道。Windows MSVC 构建与 Mac↔Windows 双机剪贴板物理验收待完成。

## 1. 记录规则

本文件用于记录每日计划、实际完成、验证证据、阻塞、风险和下一步。维护规则：

1. 每个工作日开始时增加当日计划，结束时更新实际结果。
2. 只记录已经验证的完成项；“写完但未运行”不能标记为测试通过。
3. 完成项尽量附文件、测试命令、提交哈希或性能报告。
4. 阻塞项必须写明责任人/所需决策和下一次检查时间。
5. 架构或技术栈变化同步更新 `实现方案.md`、`可行性分析.md` 和本文件的决策记录。

状态标记：

- `[ ]` 计划中
- `[-]` 进行中
- `[x]` 已完成并有证据
- `[!]` 受阻
- `[~]` 调整或取消，并说明原因

## 2. 当前总体状态

| 工作流 | 状态 | 当前结果 | 下一门禁 |
| --- | --- | --- | --- |
| 产品范围 | 已完成 | PC 完整被控 + 全平台控制端；移动被控条件化 | 原型用户验证 |
| 竞品研究 | 已完成 | 向日葵、ToDesk、Parsec、TeamViewer、RustDesk 等分析 | 按季度复核 |
| 总体架构 | 已完成 v0.2 | Flutter + Rust + 原生适配 + Java/Spring + WebRTC/coturn | 阶段 0 原型验证 |
| 可行性 | 已完成 v0.2 | 有条件可行；明确平台、成本、安全和周期风险 | 原型数据修正 |
| 项目治理 | 已完成 | README、AGENT、进展日志、Git 基线 | 首次代码变更流程 |
| 工程脚手架 | 本机基线完成 | Java、Flutter、Rust、Proto、infra、FFI 和统一命令已建立 | Windows/Linux 原生构建 |
| Apple 纵向原型 | 进行中 | 基本画面/输入已验证；Active/Pending/Retired 热切换、四层显示几何、无内部黑边的 NV12 规范画布、呈现/触控共用变换、稳定采集上限、Sender-only 自动画质、事件遮罩和双键盘已编码 | 物理设备复验 50 次交叉切屏、Retired 释放、直接触控九宫格、失败回滚、自动升降档、两种键盘、SDR/HDR、Sidecar 清晰度、60 FPS P95、GPU 峰值、多屏热插拔和 30 分钟稳定性 |
| Windows 双向原型 | 进行中 | Windows→Mac控制端已初验；Mac→Windows单主屏能力、桌面直接IME、桌面双工DNS-SD、Windows单一Texture几何和原地会话修复已编码 | Windows MSVC构建、副屏比例/九宫格、Mac拼音/DNS-SD和双向30分钟实机稳定性；再进入多显示器 |
| Java 控制平面 | 原型信令通过 | 5 分钟连接码 TTL、单次消费、邀请 5 次/分钟 + 来源 20 次/分钟两级限流、retryAfter；9 个测试通过 | 身份、独立短期票据、Redis 分布式限流/路由和 WSS |
| Flutter 客户端 | Apple 优化构建通过 | WebRTC、Bonjour、共享恢复、四层显示几何、Sender-only自动画质、Windows语义特殊键与远端IME、桌面双工发现/诊断、原地会话修复、一次性连接码自动补发、Mac/Windows文本剪贴板策略与状态 UI；124 项 Flutter 测试通过、1 项按环境跳过 | Windows MSVC与双机发现/副屏几何/输入/画质/剪贴板验收；物理 iPad 继续回归切屏、触控、键盘、色彩和 1080p60 稳定性 |
| Rust 共享核心 | 传输阶段1完成 | 6 crate；传输状态机/限额/Manifest与路径/恢复位图/SHA-256/WebRTC背压抽象/任务C ABI；fmt、Clippy和27项workspace测试通过 | `transfer-control`/`file-n`、平台文件 IO 与断点续传互操作 |
| Protobuf | 数据协议v1完成 | 新增clipboard/transfer；显式能力协商和旧客户端降级；Buf lint；Java/Rust/Dart生成和编译通过 | 平台互操作、fuzz/property和breaking基线 |
| 本地基础设施 | 本机健康 | PostgreSQL、Redis、coturn Compose 均健康 | 生产安全配置和可观测性 |
| 安全与测试 | 基线完成 | 威胁模型 v0.1、统一 `check-all.sh` 通过 | M1 安全控制与性能自动化 |

不使用一个主观“总体百分比”掩盖差异：M0 基线已完成，第一条 Apple 会话链路已编码和构建，但真实远程画面与系统输入尚需持续双设备验收；Mac↔Windows 文本剪贴板已接入代码与 UI，待 Windows MSVC 和双机物理验收；文件传输、音频和无人值守仍未完成。

## 3. 里程碑

| 里程碑 | 目标 | 状态 | 退出条件 |
| --- | --- | --- | --- |
| M0 立项与准备 | 技术栈、边界、文档、Git、实验计划 | 已完成 | 本机统一基线通过 |
| M1 Apple 纵向原型 | iPad→Mac 画面、触控、权限和稳定性 | 进行中 | 1080p60；LAN P95 ≤ 80ms、目标 ≤60ms；30 分钟稳定 |
| M1B Windows 双向原型 | 先完成Windows→Mac控制端，再实现DXGI→硬编与Windows输入注入 | 进行中 | 双向1080p30稳定；键盘/全屏/多屏通过；再提升1080p60 |
| M2 Windows MVP | 设备、授权、远控、文件、剪贴板、会话记录 | 未开始 | 50 台内测；连接成功率 ≥ 98% |
| M3 macOS 与移动控制端 | macOS 主被控、iOS/Android 控制端 | 未开始 | 权限、触控、硬解和商店检查通过 |
| M4 Linux 与 Android 受限被控 | 支持矩阵内的 Linux/Android 能力 | 未开始 | 指定发行版/设备矩阵通过 |
| M5 企业与规模化 | RBAC/SSO、审计、私有部署、多区域 | 未开始 | 安全、容量和灾备演练通过 |

## 4. 2026-08-17 每日记录

### 今日计划

- [x] 调研成熟远程桌面产品的主要场景和优势。
- [x] 编写 CrossDesktopRemote 实现方案。
- [x] 编写可行性分析、选型理由和分阶段计划。
- [x] 冻结 Flutter UI 与 Java/Spring Boot 控制平面决策。
- [x] 建立 README、协作规则和每日进展记录。
- [x] 初始化本地 Git 管理基线并暂存当前项目文件。
- [x] 建立 monorepo 目录边界和可重复执行的 bootstrap 脚本。
- [x] 通过 Spring Initializr 生成 Java 控制平面并完成编译验证。
- [x] 记录 Flutter、Rust、Protobuf 缺失工具及后续拉取步骤。
- [x] 生成 Flutter 五平台工程和 Rust workspace。
- [x] 安装 Protobuf Compiler 并复核所有工程生成结果。
- [x] 对 Flutter、Rust、Java、Docker 执行一次真实验收。
- [x] 完成 Flutter 最小 Widget 测试与 macOS Debug 构建。
- [x] 建立并验证 PostgreSQL、Redis、coturn 本地 Compose。
- [x] 建立 Java local/test 配置、Flyway V1、安全基线和健康检查。
- [x] 建立 Protobuf v1 与 Java/Rust/Dart 统一生成流程。
- [x] 建立 Flutter 响应式壳层、Rust 窄 C ABI 和最小 FFI 闭环。
- [x] 构建 Android 三 ABI Rust 动态库并打包进 Debug APK。
- [x] 建立统一 `dev-up.sh`、`check-all.sh` 并通过全流程验收。
- [x] 完成 iOS Simulator Debug 构建和威胁模型 v0.1。
- [x] 按单人和现有设备条件将首个纵向原型调整为 iPad 控制 Mac。
- [x] 通过 Flutter 命令引入 `flutter_webrtc 1.6.0`。
- [x] 建立开发态六位房间码、双角色、有界消息 WebSocket 信令。
- [x] 建立 WebRTC Offer/Answer/ICE、Mac 捕获入口和 iPad 视频视图。
- [x] 建立 DataChannel 触控消息与 Mac Accessibility/CGEvent 输入桥。
- [x] 完成 macOS、iOS Simulator、iOS 真机 arm64 构建。
- [x] 将开发版安装并启动到物理 iPad。
- [x] 用户确认物理 iPad 能连接 Mac 并看到 Mac 画面。
- [x] 将会话控制器提升到 `HomeShell`，使用 `IndexedStack` 保留页面和连接状态。
- [x] 增加 Mac 多网卡 IPv4 枚举、过滤、排序和复制入口。
- [x] 建立跨平台 `LanDiscoveryService`，完成 macOS 发布和 iPadOS 发现/解析。

### 实际完成

| 完成项 | 结果 | 证据 |
| --- | --- | --- |
| 竞品研究 | 完成场景、优势和产品启示对比 | `实现方案.md` 第 2 节 |
| 技术方案 | 完成架构、协议、性能、文件、安全和实施计划 | `实现方案.md` v0.2 |
| 可行性 | 完成平台、性能、成本、许可、团队和风险研究 | `可行性分析.md` v0.2 |
| 技术决策 | Flutter 取代 Electron；Java 取代 Go；coturn 保持独立 | 两份方案文档的技术栈与决策记录 |
| 项目入口 | 增加架构、功能、平台、目标启动与部署说明 | `README.md` |
| 协作治理 | 增加架构红线、安全、测试、Git 和日志规则 | `AGENT.md` |
| Git 基线 | 本地仓库使用 `main` 分支；调研、设计和工程基座已提交 | commits `3e70ed5`、`a98fcdf` |
| 仓库骨架 | 建立 apps、crates、platform、services、infra、proto、tests、docs、scripts | `find ... -maxdepth 3 -type f` |
| Java 骨架 | Spring Initializr 官方生成 Spring Boot 4.1.0/Gradle 工程 | `services/control-plane-java` |
| 生成流程 | 增加 Flutter、Rust、Spring 和工具链检查脚本 | `scripts/`、`docs/工程搭建.md` |
| Flutter 骨架 | 生成 Android、iOS、Linux、macOS、Windows 工程 | `apps/client_flutter` |
| Flutter 测试 | 增加首屏 Widget 测试并完成 macOS Debug 构建 | `test/main_app_test.dart`、Flutter 构建输出 |
| Android 构建 | 完成 NDK/Platform 36 安装并生成 Debug APK | `flutter build apk --debug`、`build/app/outputs/flutter-apk/app-debug.apk` |
| iOS 构建 | 完成 iOS Simulator Debug 构建 | `flutter build ios --simulator --debug` |
| Flutter 应用壳层 | 建立设备、会话、设置页面和移动/桌面响应式导航 | `apps/client_flutter/lib/app`、`lib/features` |
| Rust workspace | 生成 6 个 crate，建立版本、帧元数据、传输限额和独立权限集合等模块边界 | `Cargo.toml`、`Cargo.lock`、`crates/` |
| Rust/Flutter FFI | 建立版本/能力 C ABI，macOS 动态库真实加载测试通过 | `crates/client-ffi`、`lib/core/bridge` |
| Android Rust | 为三个 Android ABI 构建 `.so` 并打包进 APK | `scripts/build-rust-core-android.sh`、APK 内容检查 |
| Protobuf 工具 | 安装 Protobuf Compiler 35.1 | `protoc --version` |
| Protobuf v1 | 建立六组基础消息（含 input）、Buf 规则和 Java/Rust/Dart 生成 | `proto/crossdesktop/v1`、`scripts/generate-proto.sh` |
| 本地基础设施 | PostgreSQL、Redis、coturn 三个容器健康 | `infra/deploy/compose.dev.yaml`、`scripts/dev-up.sh` |
| Java 基座 | Flyway V1、local/test 配置、健康检查和默认拒绝策略通过 | `services/control-plane-java`、`gradlew test` |
| 安全基线 | 建立资产、信任边界、威胁和 M1 门禁 | `docs/威胁模型.md` |
| 统一验收 | infra、Proto、Rust、Java、Flutter、macOS/Android 全流程通过 | `scripts/check-all.sh` |
| Apple 原型路线 | 单人优先完成 iPad 控制 Mac，再进入 Windows 性能原型 | `实现方案.md` 阶段 0A/0B、`可行性分析.md` |
| WebRTC 客户端 | Mac Host/iPad Controller、SDP/ICE、原生视频视图和触控 DataChannel | `lib/features/remote`、`lib/core/signaling` |
| Mac 输入桥 | Accessibility 权限提示和归一化鼠标/滚轮事件注入 | `macos/Runner/MainFlutterWindow.swift` |
| Java 信令 | 六位房间码、双角色、消息白名单、64KiB 上限与双连接中继 | `services/control-plane-java/.../signaling` |
| 物理 iPad | 自动签名、安装和应用启动成功 | `flutter run -d 00008103-001674902207001E`、`devicectl` |
| 会话持久化 | `HomeShell` 持有会话，导航页使用 `IndexedStack`，会话页显示进行中状态 | `home_shell.dart`、`main_app_test.dart` |
| 局域网地址 | 区分内部回环地址与 iPad 可用地址，优先 Wi-Fi/有线网卡 | `lan_address_service.dart` |
| Apple 自动发现 | `_cdrremote._tcp` DNS-SD 发布/浏览/解析，房间码不进入 TXT | `lan_discovery_service.dart`、Apple Runner Swift |
| 跨平台发现任务 | 记录 Android `NsdManager`、Windows DNS-SD 和 Linux Avahi 适配矩阵 | `docs/局域网发现与跨平台适配.md` |
| Mac 输入权限修复 | 使用 `CGRequestPostEventAccess`/`CGPreflightPostEventAccess`，返回前台自动复查并向控制端发布状态 | `MainFlutterWindow.swift`、`mac_input_bridge.dart`、`remote_session_controller.dart` |
| 输入与键盘反馈 | 增加输入 ACK、错误回传、可见远程输入栏和全屏常驻键盘入口 | `remote_session_controller.dart`、`remote_desktop_panel.dart` |
| 画面与清晰度 | 画布脱离远端固定宽高比；支持适应/填满及自动、720p、1080p、2K、原画发送参数 | `remote_desktop_panel.dart`、`remote_session_models.dart` |
| 平台角色解耦 | 增加设备能力模型，Mac 可切换控制端/被控端；UI 文案改为设备和角色语义 | `device_capabilities.dart`、`home_shell.dart`、`devices_page.dart` |
| 应用消息反馈 | 建立全局浮动 `SnackBar`，覆盖连接、权限、发现、输入、画质、显示器、复制和错误结果 | `app_messenger.dart` |

### 今日验证

- [x] 两份核心方案中的 UI 和服务端技术栈保持一致。
- [x] README 准确区分工程生成、检查通过和运行阻塞，未伪造运行结果。
- [x] `bash -n scripts/*.sh` 通过。
- [x] Java `gradlew tasks` 返回 `BUILD SUCCESSFUL`。
- [x] Java `compileJava compileTestJava` 返回 `BUILD SUCCESSFUL`。
- [x] Gradle 弃用警告已定位为用户级 `~/.gradle/init.gradle`，不是项目构建文件。
- [x] Rust fmt、Clippy、workspace test 全部通过，6 个测试成功。
- [x] Flutter `pub get` 与 `analyze` 通过。
- [x] Flutter `test` 通过：11 个常规测试和 1 个真实 macOS dylib FFI 测试成功。
- [x] Flutter macOS Debug 构建成功。
- [x] Flutter iOS Simulator Debug 构建成功。
- [x] Flutter Doctor：Android SDK 36.0.0、Xcode 26.6、CocoaPods 1.17.0 通过。
- [x] Flutter Android Debug APK 构建成功；用户级镜像保持不变，项目级 Settings 仅向 JitPack 开放 `com.github.davidliu` 传递依赖。
- [x] Java `test`、`local` profile 启动、Flyway V1 和 `/actuator/health` 通过；此前缺数据源问题已解决。
- [x] Docker daemon 已运行；Compose 配置有效，PostgreSQL、Redis、coturn 均健康。
- [x] Protobuf v1 的 Buf lint、Java/Rust/Dart 生成和编译/分析通过。
- [x] Android 三 ABI 动态库已生成并确认打包进 Debug APK。
- [x] `scripts/check-all.sh` 全流程通过。
- [x] `scripts/check-toolchains.sh` 已识别 Flutter、Rust、Protobuf、Buf、Dart 插件和 cargo-ndk，未报告缺项。
- [x] 最终 `bash -n scripts/*.sh` 与 `git diff --check` 通过。
- [x] Git 初始提交已按用户明确要求完成，未执行 push。
- [x] Java WebSocket 双端中继集成测试通过；控制端在 Host 注册前会被拒绝。
- [x] Flutter `analyze` 无问题；11 个常规测试通过，Rust FFI 测试在未传动态库路径时按设计跳过。
- [x] WebRTC 依赖接入后 macOS Debug、iOS Simulator Debug 和 iOS 真机 arm64 构建通过。
- [x] 物理 iPad 开发版安装成功，并通过 `devicectl` 启动。
- [x] Apple 原型首次接入后的 `scripts/check-all.sh` 全流程通过：含 Rust 6 测试、Java 5 测试、Flutter 6 测试和 macOS/iOS Simulator/Android 构建。
- [x] 会话保活、地址选择和发现数据新增 6 个测试；macOS 发布与 iOS Simulator 发现原生代码构建通过。
- [x] 本次优化后 `scripts/check-all.sh` 再次全流程通过：Rust 6、Java 5、Flutter 12 测试及 macOS/iOS Simulator/Android Debug 构建全部成功。
- [x] 优化版完成 iOS 真机签名构建，已覆盖安装并启动到物理 iPad；Mac Debug 应用和最新 Java 信令服务已重启。
- [x] 连接码改为 Host 先注册、5 分钟 TTL、单次消费和每来源一分钟 5 次失败限流；正确码由 Mac 自动授权，不再二次点击。
- [x] 视频画布改为按 renderer 实际尺寸布局，新增适应/填满、黑边坐标排除和沉浸全屏。
- [x] 新增触控板/直接触控、左/右键、双击、拖拽、双指滚动、外接鼠标、软键盘和常用快捷键事件。
- [x] Mac 原生桥新增辅助功能状态、显示器枚举、displayId 坐标映射、相对指针和 Unicode/虚拟键注入。
- [x] 新增显示器列表、手动刷新、热插拔事件监听和 `replaceTrack()` 单路无重连切换。
- [x] 新增 `input.proto`，完成 Buf lint、Java/Dart 生成和 Rust protocol crate 编译。
- [x] 本轮 Flutter `analyze` 无问题；14 个常规测试通过，统一脚本设置动态库后真实 Rust FFI 测试也通过（合计 15）。
- [x] 本轮 Java 8 个测试通过；macOS Debug、iOS Simulator Debug 和 iOS 真机自动签名 arm64 构建通过。
- [x] 本轮 `scripts/check-all.sh` 全流程通过：Docker 三服务健康、Proto/Buf、Rust 6、Java 8、Flutter 15、macOS/iOS Simulator/Android Debug 全部成功。
- [x] 当前 Java `local` 服务已用新代码重启且健康状态为 `UP`；最新版 Mac Debug 已打开，iPad 经 `flutter run` 重新签名安装并由 `devicectl` 启动成功。
- [x] 输入权限改用 macOS 事件发布专用 API，macOS Debug 原生编译通过。
- [x] 新增清晰度模型与全局消息测试；本轮 `flutter analyze` 无问题，16 个常规测试通过，1 个未配置动态库的 FFI 测试按设计跳过。
- [x] 本轮 `flutter build macos --debug` 与 `flutter build ios --debug --no-codesign` 均通过。

### 今日阻塞与待验证

- 当前 Codex 非交互 Shell 可能不继承 Flutter/Rust PATH，统一脚本已支持显式工具路径，不再阻塞构建。
- 用户级 `~/.gradle/init.gradle` 使用 `PREFER_SETTINGS` 和国内镜像，会影响本机全部 Gradle 工程；它只作为本机网络适配，不能成为仓库的隐式运行依赖。
- Windows/Linux 必须在对应操作系统上完成原生构建，macOS 不能替代该验证。
- Android `.so` 已打包但未做真机运行；macOS dylib 尚未自动嵌入/签名到应用包。
- Mac 屏幕录制、辅助功能和 iPad 局域网访问必须由用户在系统界面授权，无法在自动构建中验证。
- 物理 iPad 首次由 `flutter run` 启动时未发现 Dart VM Service，但应用已确认安装，并通过 `devicectl` 成功启动；这不影响 Release 路线，但后续需要确认 USB 调试会话。
- 开发态连接码 TTL/单次消费/限流仅存在单 JVM 内存中；仍无设备身份、独立短期票据、Redis 跨实例路由或 WSS，只允许局域网原型使用。
- 自动化无法替代物理触控、系统权限弹窗、两块真实显示器切换/热插拔和 30 分钟稳定性验收。

## 5. 2026-08-26 每日记录

### 今日计划

- [x] 将信令服务器地址提升为跨角色持久配置，增加手动可达性检查。
- [x] 将连接码生成与轮换收口到 Java 信令服务。
- [x] 将 DNS-SD 设备发现与信令 rendezvous 配置解耦。
- [x] 增加 Mac 控制 Windows 时使用 Windows 系统输入法的可选路径。
- [-] 完成 Windows MSVC 构建和 Mac/Windows 双机物理验收。

### 实际完成

| 完成项 | 结果 | 验证证据 |
| --- | --- | --- |
| 信令配置 | `SharedPreferencesAsync` 持久化，切换控制/被控角色不丢失；增加 TCP 可达性检查 | `app_settings_controller.dart`、`devices_page.dart` |
| 连接码 | 主机无需预生成房间码；Java 返回 code/lease/expiry，同一 WebSocket 原子轮换并作废旧码 | Java registry/handler 单元与集成测试 |
| 发现 v2 | TXT 新增 platform/signal/signalUrl；设备地址不再无条件覆盖当前信令服务器 | Dart、macOS、iOS、Windows 原生适配 |
| 远端 IME | 增加“被控端输入法”；可打印键改走 USB HID，由 Windows 输入法组词 | 键盘翻译器测试，Windows 物理输入待实机验收 |

### 今日验证

- [x] Flutter `analyze` 通过。
- [x] Flutter 102 项测试通过，1 项 FFI 按环境跳过。
- [x] Java `./gradlew test` 通过，包含主机无房间码接入和同 Socket 轮换集成测试。
- [x] `flutter build macos --debug` 通过。
- [x] `flutter build ios --debug --no-codesign` 通过。
- [-] Windows 发现原生代码需在 Windows 11 + MSVC 环境构建，不在 macOS 伪造验证结论。

## 6. 两个工作周滚动计划

以下日期是建议计划，负责人和实际排期确定后允许调整；调整必须保留原因。

| 日期 | 每日目标 | 计划产物 | 状态 |
| --- | --- | --- | --- |
| 2026-08-18 | 建立 monorepo 工程骨架与工具链约束 | Flutter、Rust、Java、proto、infra 工程；版本说明 | `[x]` 提前完成并通过统一验收 |
| 2026-08-19 | 定义协议 v1 与会话状态基线 | Protobuf 消息、错误码、capability、生成流程 | `[x]` 提前完成；状态机业务待 M1 迭代 |
| 2026-08-20 | 建立 Java 控制平面骨架 | Spring Boot、健康检查、PostgreSQL/Redis、本地 Compose | `[x]` 提前完成 |
| 2026-08-21 | 建立 Flutter 客户端骨架 | 桌面/移动入口、路由、主题、设备页占位 | `[x]` 提前完成 |
| 2026-08-24 | 建立 Rust workspace | session/protocol/media/transfer/security/client-ffi crates | `[x]` 提前完成 |
| 2026-08-18 | Apple 原型工程与信令 | `flutter_webrtc`、房间码、Java WebSocket、Apple 权限 | `[x]` 提前完成编码/构建 |
| 2026-08-19 | Mac→iPad 画面人工验收 | 720p30 主显示器、原生视频视图、10 分钟运行 | `[-]` 等待系统权限和双设备操作 |
| 2026-08-20 | iPad→Mac 输入验收 | 点击、拖动、滚动、键盘、坐标映射 | `[-]` v2 代码和 Apple 构建完成，等待人工验收 |
| 2026-08-21 | 连接生命周期 | 跨页保活、Host 共享自动恢复和旧连接回调隔离已编码/测试；拒绝、断开、重连和资源释放 | `[-]` 等待物理双设备复验 |
| 2026-08-24 | 1080p60 性能优化 | 帧率、码率、延迟、CPU/GPU 和 30 分钟报告 | `[ ]` |
| 2026-08-25 | TURN 与弱网 | P2P/Relay、网络切换和自动降级 | `[ ]` |
| 2026-08-26 | M1 Apple 评审 | 验收报告、插件保留/替换决策、Windows M1B 输入 | `[ ]` |

## 7. 当前待办池

### P0 — 阶段 0 必须完成

- [~] 指定 Windows 原型硬件：延后到 Apple 纵向原型通过后；现有 Windows PC 用于 M1B。
- [-] 固定 JDK、Flutter、Rust、Gradle 和平台 SDK 版本：macOS/Android 已记录，Windows/Linux 待验证。
- [x] 创建 monorepo 脚手架和统一构建入口：`dev-up.sh`、`generate-proto.sh`、`check-all.sh`。
- [x] 定义 Protobuf v1 基线与三语言生成流程；breaking/互操作测试随协议演进补充。
- [-] 建立 Java、Redis 和 coturn 最小环境：开发态 WebSocket 信令通过，TURN 凭据和 Redis 路由待接入。
- [-] 建立 iPad→Mac WebRTC→原生视频视图→完整输入闭环：自动授权、专用输入权限、触控/键盘、画质和多屏代码及构建完成，等待双设备验收。
- [-] 建立局域网设备发现：Apple DNS-SD 编码/构建完成，等待物理 iPad 验收；其他平台按任务矩阵实施。
- [~] 建立 DXGI→H.264→WebRTC→硬解→原生纹理闭环：调整到 M1B。
- [ ] 建立输入到画面响应的硬件/软件双测方法。
- [x] 完成初版威胁模型和高权限进程边界。

### P1 — Windows MVP

- [ ] 用户登录、MFA 和可信设备。
- [ ] 设备注册、在线状态与设备绑定。
- [ ] 有人/无人值守授权。
- [ ] 文本剪贴板。
- [ ] 原文件传输、校验和断点续传。
- [ ] 设备信息、授权截图和会话元数据。
- [ ] Windows Service 与交互 Session Worker。
- [ ] 安装、签名、自动更新和恢复策略。

### P2 — 后续平台

- [ ] macOS ScreenCaptureKit/VideoToolbox 与权限引导。
- [-] iOS/iPadOS 控制端触控、键盘和文件选择：原生 IME 代理、触控和输入反馈已编码并构建，文件选择待后续，原生 IME 待真机验收。
- [ ] Android 控制端及受限被控。
- [ ] Linux Wayland Portal/PipeWire 和 X11 后备。
- [-] 多显示器、2K60、条件化 4K30/60：macOS Active/Pending/Retired 采集热切换、自动/720p/1080p 60 FPS 上限已编码并构建；2K60、4K 和多路平铺仍需性能门禁。
- [ ] Android 使用 `NsdManager` 实现 `_cdrremote._tcp` 发现，兼容 MulticastLock 和新版本局域网权限。
- [ ] Windows 使用 DNS Service Discovery API 实现发布/发现并验证防火墙。
- [ ] Linux 使用 Avahi 实现发布/发现，无 daemon 时降级手动输入。

## 7. 风险与阻塞跟踪

| ID | 风险 | 当前等级 | 当前应对 | 状态 |
| --- | --- | --- | --- | --- |
| R-001 | Flutter 原生纹理/多窗口影响低延迟 | 高 | 阶段 0 验证；帧不进入 Dart；必要时会话视图局部原生化 | 开放 |
| R-002 | 团队缺少 Flutter 经验 | 中 | 组件/插件模板、培训、code owner、减少首版自定义渲染 | 开放 |
| R-003 | Java WebSocket 阻塞或无界队列 | 高 | 事件循环隔离、消息/队列上限、慢客户端断开、1 万连接压测 | 开放 |
| R-004 | 4K60 硬件与温控不稳定 | 高 | capability 白名单和自动降级，不进入 MVP | 开放 |
| R-005 | NAT/CGNAT 导致 P2P 失败 | 高 | ICE + 区域 coturn + TCP/TLS 443 兜底 | 开放 |
| R-006 | 高权限 Agent 安全漏洞 | 极高 | 最小进程、IPC 鉴权、签名、fuzz 和第三方测试 | 开放 |
| R-007 | RustDesk/编解码开源与专利许可 | 高 | clean-room、SBOM、法律审查和商业许可预算 | 开放 |
| R-008 | mDNS 被伪造或被网络禁用 | 中 | 不将发现视为身份；保留短时单次连接码、失败限流和手动地址 | 开放 |
| R-009 | 可靠输入与低时效移动跨 DataChannel 乱序 | 高 | 状态/移动独立序列门禁；按下/抬起可靠传输；取消与断线强制释放 | 已控制，待真机弱网复验 |
| R-010 | iOS 输入法组合态或第三方输入法行为不同 | 中 | UIKit 原生 IME 代理；Apple 拼音完整 CJK 候选即时提交；本地编辑兼容入口；隐私安全诊断和物理输入法矩阵 | 已降低，待真机矩阵复验 |
| R-011 | 动态 filter 保留旧 Sidecar 内容面或切换事务资源未释放 | 高 | 目标绑定的新 `SCStream` 双帧预热；Active/Pending/Retired 两阶段提交；固定 Track/Sender/MID/SSRC；瞬时 `Disconnected` 进入 8 秒宽限 | 代码已修复并构建，待 30 次物理切屏和 Instruments 资源检查关闭风险 |

## 8. 技术决策记录

| 日期 | 决策 | 原因 | 状态 |
| --- | --- | --- | --- |
| 2026-08-17 | PC 完整被控、全平台控制，移动被控条件化 | 遵守 Android/iOS/Wayland 的平台权限边界 | 已接受 |
| 2026-08-17 | 使用 Flutter，不使用 Electron | 同一 UI 体系覆盖桌面、手机和平板；媒体仍走原生纹理 | 已接受 |
| 2026-08-17 | 使用 Java/Spring Boot，不使用 Go | 复用现有 Java 人员；控制面不在媒体热路径 | 已接受 |
| 2026-08-17 | WebRTC 首发、QUIC 预留 | 优先获得 ICE/TURN、拥塞、安全和跨端成熟实现 | 已接受 |
| 2026-08-17 | coturn 独立部署 | 避免使用 Java 重写成熟的 UDP 中继数据面 | 已接受 |
| 2026-08-17 | MVP 先做 1080p60 | 先验证端到端低延迟，再扩 2K/4K 和平台 | 已接受 |
| 2026-08-17 | 单人开发先做 iPad 控制 Mac | 利用现有 Mac/iPad 和 Apple 工具链，最早形成真实纵向成果；Windows 性能路线后移但不取消 | 已接受 |
| 2026-08-17 | M1 原型使用 `flutter_webrtc 1.6.x` | 降低单人直接维护 libwebrtc/双平台纹理的成本；帧仍留在原生层，后续按数据决定替换范围 | 已接受，需阶段复核 |
| 2026-08-17 | 局域网发现统一使用 DNS-SD/mDNS | 避免子网扫描；Apple/Android/Windows/Linux 均有系统级实现 | 已接受，Apple 原型已实现 |
| 2026-08-17 | 临时连接码验证后自动授权 | 减少 Mac 二次确认；用 5 分钟 TTL、单次消费和每来源限流控制原型风险 | 已接受，仅限 LAN 原型；生产需设备身份/票据/WSS |
| 2026-08-17 | M1 多屏使用单轨 `replaceTrack()` | 避免重连和移动端同时解码多路视频，适合单人原型 | 重新接受；尺寸适配下沉到原生视频源 |
| 2026-08-17 | 输入权限使用 Core Graphics PostEvent API | 当前只需发布 `CGEvent`，避免用完整 Accessibility 状态错误拦截输入 | 已接受；物理 Mac 待验收 |
| 2026-08-17 | 平台能力与会话角色分离 | Mac 同时具备控制和共享能力；Windows/Linux 未完成的 Host 能力不提前开放 | 已接受；当前先支持单角色切换 |
| 2026-08-17 | 清晰度先修改 WebRTC Sender 参数 | 无需重连即可验证720p/1080p/2K/原画；后续再接自适应统计和原生采集尺寸 | 已接受；真实码率待测 |
| 2026-08-18 | Host 共享意图独立于单次会话 | 控制端断开不应要求 Mac 再次点击；自动换用新的单次码，同时保留显式停止和有限重试 | 已接受；物理双设备待复验 |
| 2026-08-18 | 仓库内维护 `flutter_webrtc` fork | 上游 1.6.0 macOS 采集硬编码 Full-Range NV12 且未明确 Rec.709；不能修改用户 Pub 缓存 | 已接受；固定 SDR/420v/BT.709，待 HDR 物理验收 |
| 2026-08-18 | 控制端显示调整不替代采集修复 | 颜色矩阵可改善观感但不能恢复已裁切高光 | 已接受；采集端优先，Metal tone mapping 按验收结果触发 |
| 2026-08-24 | 多屏切换采用单轨混合重配置 | `replaceTrack()` 未保证主屏/Sidecar 尺寸切换后编码器立即重配；同尺寸保留快速路径，尺寸变化时重建 Sender 并在原 PeerConnection 内重新协商 | 已撤销；物理测试发现该路径会触发连接断开 |
| 2026-08-24 | 多屏固定单 Sender并增加断线宽限 | 普通分辨率变化不应改变 transceiver/MID/会话；尺寸适配由原生视频源处理；WebRTC `Disconnected` 可能是瞬时状态 | 已编码并自动验证，待物理连续切屏验收 |
| 2026-08-24 | macOS 多屏改为单 `SCStream` 原地切换 | `replaceTrack()` 兼容路径会短时保留双采集，编码尺寸也不保证等于目标物理像素；ScreenCaptureKit 支持动态更新 configuration/filter | 已撤销；物理诊断证明 filter 回调成功后仍可能持续输出旧 Sidecar 内容面 |
| 2026-08-25 | 显示器切换采用 Active/Pending/Retired 采集事务 | 新目标流在构造时绑定显示器，预热成功才接管同一 RTC Source；保留原流可安全回滚，不重建 RTC Track/Sender/连接 | 已编码；静态分析、73 项测试和 macOS/iOS Debug 构建通过，待物理 30 次往返与资源验收 |

## 9. 2026-08-18 每日记录

### 今日计划

- [x] 定位直接触控、触控板文字选择和鼠标漂移原因。
- [x] 修复 iOS 拼音组合态并提供本地编辑后发送兼容入口。
- [x] 合并高频指针事件并拆分低时效移动通道。
- [x] 增加右键、双击拖动、拖拽锁定、操作说明和灵敏度设置。
- [x] 增加自动测试、Apple 构建和物理设备验收说明。
- [x] 取消 iPad 输入框与 Mac 文本的历史镜像，改为 iOS 原生 IME 输入事件代理。
- [x] 将中文提交、退格、Enter 和 Tab 拆为独立输入事件，补充候选即时提交与隐私安全诊断。
- [x] 放大 macOS 默认窗口并设置合理的最小内容区。
- [x] 将 Host 共享意图与单条 WebRTC 会话拆分，会话结束后自动生成新连接码并恢复等待。
- [x] 修复“本地编辑后发送”控制器生命周期，并增加连续 30 次打开/取消/发送回归。
- [x] 将全局 Message 改为安全区下方约 17% 高度的根 Overlay。
- [x] 增加色彩格式、Range、BT.709 附件、HDR/EDR 诊断和远程灰阶图入口。
- [x] 增加按设备/显示器保存的自动、标准 SDR、柔和高光和自定义显示调整。
- [x] 建立项目内 `flutter_webrtc` fork，明确 macOS SDR/Rec.709/Video-Range NV12 采集。

### 实际完成

| 完成项 | 结果 | 证据 |
| --- | --- | --- |
| 中文输入 | 删除零宽字符哨兵；IME composing 期间不发送，候选提交后只发送最终 Unicode；按字素处理删除和按 256 UTF-16 单元分块 | `remote_text_input_synchronizer.dart`、`remote_text_chunks.dart` |
| 中文诊断与兜底 | 远程键盘增加“组合文本发送”；Debug 输入诊断可固定发送“你好” | `remote_desktop_panel.dart` |
| 触控手势 | 直接触控增加双指右键、双击第二下拖动和长按/拖选消歧；触控板增加双击拖动、拖拽锁定和左右键栏 | `remote_touch_gesture_controller.dart`、`remote_desktop_panel.dart` |
| 操作引导 | 首次切换模式显示说明；工具栏可随时查看操作说明、调整指针/滚动速度和检查输入诊断 | `remote_desktop_panel.dart` |
| 指针低延迟 | 移动/滚动约 60Hz 合并；绝对位置保留最新值，相对位移累加；缓冲超过 64KiB 时丢弃过期移动 | `remote_pointer_event_coalescer.dart`、`remote_session_controller.dart` |
| 通道拆分 | 新增无序、50ms 重传寿命的 `input-motion` DataChannel；按键和按钮状态继续使用可靠 `control` | `remote_session_controller.dart` |
| 跨通道时序 | 可靠状态和低时效移动使用独立接收序列门禁，防止移动事件抢先到达后丢弃鼠标按下/键盘事件 | `remote_input_sequence_guard.dart` |
| 释放保护 | 触摸取消、模式切换、断开会话和 DataChannel 关闭时释放鼠标按钮；Mac 原生层提供最终释放兜底 | `remote_desktop_panel.dart`、`mac_input_bridge.dart`、`MainFlutterWindow.swift` |
| 协议 | `input.proto` 增加输入时间戳、延迟探针和输入回执，重新生成 Java/Dart/Rust 绑定并通过 Buf/Cargo 检查 | `proto/crossdesktop/v1/input.proto`、`scripts/generate-proto.sh` |
| 验收说明 | 增加直接触控、触控板、拼音、延迟和释放保护的物理设备检查表 | `docs/iPad控制Mac输入验收.md` |
| 原生 IME 输入代理 | 新增隐藏 `UITextView`，由 UIKit 管理 marked text；完整拼音候选提交后清空本机缓冲，不保存远端文本历史 | `RemoteImeBridge.swift`、`remote_ime_input_adapter.dart` |
| 删除与生命周期 | 未组合时 `deleteBackward` 直接发送远程 Backspace；组合中退格只修改本机候选；页面/会话/后台切换取消组合并按 clientId 隔离事件 | `RemoteImeBridge.swift`、`remote_desktop_panel.dart` |
| 输入诊断 | Debug 键盘栏可查看事件类型、组合长度和 CJK 标记；诊断不记录实际输入内容 | `remote_desktop_panel.dart` |
| macOS 窗口 | 默认内容区调整为 `1180×760`，最小内容区为 `900×620`，首次打开直接使用桌面布局 | `MainMenu.xib`、`MainFlutterWindow.swift` |
| Host 会话恢复 | 已成功会话断开后保留共享意图、自动换码并恢复等待；显式停止、首次失败和二次恢复失败不会循环重试 | `host_sharing_lifecycle.dart`、`devices_page.dart` |
| 连接竞态保护 | PeerConnection 回调只处理当前且未关闭的连接，避免旧连接延迟回调污染新会话 | `remote_session_controller.dart` |
| 兼容输入稳定性 | “本地编辑后发送”由独立 Stateful 页面管理控制器，退出动画结束后再释放；30 次循环回归通过 | `remote_composed_text_editor.dart`、对应测试 |
| 全局消息 | Message 改为根 Overlay，位于安全区下方和屏幕约 17% 高度 | `app_messenger.dart`、对应测试 |
| 色彩诊断 | 显示像素格式、Range、primaries、transfer、matrix、动态范围和每屏 EDR；可远程打开灰阶图/显示器设置 | `MainFlutterWindow.swift`、`remote_session_controller.dart` |
| 显示调整 | 自动、标准 SDR、柔和高光、自定义亮度/对比度/饱和度；按远程设备/显示器持久化 | `remote_display_adjustment.dart`、`remote_desktop_panel.dart` |
| macOS SDR 采集 | 项目内 fork 使用 `420v` Video Range、BT.709 色彩空间/矩阵与附件；macOS 15+ 强制 SDR | `third_party/flutter_webrtc` |

### 验证

- [x] `flutter analyze` 无问题。
- [x] `flutter test`：44 个常规测试通过；1 个未配置动态库的既有 FFI 测试按设计跳过。
- [x] 新增中文组合态/emoji 字素、指针合并、跨通道时序和手势状态机测试。
- [x] `flutter build macos --debug` 成功。
- [x] `flutter build ios --debug --no-codesign` 成功。
- [x] 采集事务、自动档分层和 Sidecar 居中修复后再次通过 `flutter analyze`、73 项测试、macOS Debug 与 iOS Device Debug 无签名构建。
- [x] `flutter build ios --debug` 自动开发签名成功。
- [x] 新版应用已安装到物理 iPad，并确认 Bundle ID/版本存在。
- [x] iPad Simulator 上 4 个 `RemoteImeClassifier` Swift 测试通过，覆盖完整 CJK 候选、部分拼音不提前提交、CJK→CJK 不重复触发和扩展汉字。
- [x] 原生 IME 变更后的 `flutter analyze` 无问题，`flutter build ios --simulator --debug` 成功。
- [x] 原生 IME 变更后的 macOS Debug 与 iOS 真机自动签名构建成功；最新 Debug 已覆盖安装到物理 iPad，并确认 Bundle ID/版本存在。
- [x] 新增 4 个 Host 共享生命周期测试；`flutter analyze` 和 `flutter build macos --debug` 通过。
- [x] 新增消息位置、30 次兼容输入、显示矩阵/按屏隔离和色彩诊断模型测试；`flutter analyze` 无问题。
- [x] 项目内 WebRTC fork 变更后的 macOS Debug、iOS Simulator Debug 和 iOS 真机架构 Debug（`--no-codesign`）构建成功。

### 阻塞与风险

- Simulator 与单元测试不能还原 Apple/第三方中文输入法的完整候选事件顺序；最新版本已安装，仍需用户在物理 iPad 上操作验收。
- 自动测试不能判断真实触控手感、Mac 画面反馈延迟和 30 分钟资源稳定性，仍需按验收文档人工执行。
- Host 自动换码和 Bonjour 重新发布已经单元测试与 macOS 构建验证，仍需物理 iPad 确认断开后附近设备条目可再次使用，并用 Mac 显示的新连接码重新连接。
- 自动测试不能判断真实面板色彩；需按 `docs/iPad控制Mac色彩验收.md` 完成普通桌面、HDR 视频、235～255 灰阶、HDR 开关和多显示器验收。若显式 SDR 输出仍失败，再实现 Metal/Core Image tone mapping。
- `flutter_webrtc 1.6.0` 的 DataChannel 初始化只在重传寿命大于 0 时下发参数，因此 M1 使用 50ms 生命周期而不是严格零重传；正式传输实现仍按 `input-motion` 过期即丢弃设计。

### 下一工作日

- [ ] 打开物理 iPad 上的最新版本，执行 Apple 简体拼音、候选即时提交、Mac 主动删除和长按退格验收。
- [ ] 选取至少一种第三方输入法验证；不符合即时提交矩阵时确认“本地编辑后发送”兼容入口，不启用激进通用启发式。
- [ ] 记录输入往返 P50/P95、过期移动丢弃数和连续移动偏差。
- [ ] 若仍有稳定坐标偏差，增加 Mac 实际鼠标坐标回传；若仅有视觉滞后，进入视频管线延迟打点。
- [ ] 完成 30 分钟 1080p 会话、断线释放和多显示器热插拔验收。
- [ ] 执行 SDR 灰阶、HDR 开关 A/B 和主屏/扩展屏显示调整独立恢复验收。

## 10. 2026-08-24 每日记录

### 今日计划

- [x] 沿 ScreenCaptureKit 原始帧、WebRTC 编码器输入和 iPad 解码帧建立色彩检查点。
- [x] 停止用覆盖附件代替 HDR/SDR 和 Range 像素转换。
- [x] 修复 iPad Texture 路径丢失 BT.709/Video-Range 语义的问题。
- [x] 补充色彩诊断模型、UI、测试和物理验收判读规则。
- [x] 修复主屏/Sidecar 切换后的分辨率不同步、画面偏移和全屏名称不刷新。
- [x] 将被控端连接码改为可选择文本和独立复制操作。
- [x] 修复尺寸变化时重建 Sender/SDP 协商导致的切屏断线回归。
- [x] 将瞬时 `Disconnected` 与终止会话分离，增加 8 秒恢复窗口。
- [x] 恢复标准连接码控件密度，固定工具栏布局并保持 RTCVideoView 实例稳定。
- [x] 分离显示器逻辑尺寸与采集像素尺寸，按 Sidecar/Retina 点像素比例采集。
- [x] 让全屏视频相对工具栏和远程键盘以外的可用区域居中。
- [x] 重构设备页信息层级，并将高级网络信息默认收起。
- [x] 将会话页升级为实时诊断与本地元数据历史。
- [x] 将设置页升级为可持久化的画质、输入、发现和隐私设置。
- [x] 将切屏从尺寸提示改为 ScreenCaptureKit 首帧、编码帧、解码帧双端提交，失败自动回滚。
- [x] 将直接触控绑定到真实渲染显示器并修复最右/最下边缘越界。
- [x] 将连接码失败保护改为邀请/来源两级限流，并返回限流范围和剩余秒数。
- [x] 取消发送端和接收端对期望尺寸的阻塞门禁，改为媒体帧流门禁和后台画质适配。
- [x] 将 macOS 主屏/Sidecar 切换改为单 `SCStream` 动态 configuration/filter，保留旧系统兼容路径。
- [x] 增加 SDR/BT.709/Video-Range 快速路径、60 FPS 交互档和 WebRTC 媒体延迟诊断。
- [x] 增加 ScreenCaptureKit `.complete` 帧门禁、新 NV12 画布和 iPad 事件驱动切屏遮罩，隐藏 Sidecar 过渡残帧。
- [x] 保持全屏远程画布尺寸稳定，并增加可持久选择的系统完整键盘和可拖动快捷小键盘。
- [x] 曾将切屏门禁升级为强制编码/解码关键帧；真机证明该门禁在静态Sidecar和部分WebRTC构建上误拒绝正常媒体，现已降级为尽力优化。
- [x] 增加 1 秒区间媒体统计、SCK 帧几何诊断和带滞回的 1080p/720p、30/60 FPS 自动画质梯度。
- [x] 将自动档目标尺寸前移到 ScreenCaptureKit，并优先使用 VideoToolbox H.264。

### 实际完成

| 完成项 | 结果 | 证据 |
| --- | --- | --- |
| Mac 原始帧诊断 | 转换前记录真实 FourCC、Range、primaries、transfer、matrix、Y范围、越界比例和16桶直方图 | `FlutterScreenCaptureKitCapturer.m` |
| Mac 编码器输入 | Core Image/Metal 将输入真实渲染为 SDR、BT.709、Video-Range `420v`；转换成功后才写入附件 | `FlutterScreenCaptureKitCapturer.m` |
| iPad 解码检查点 | 记录解码 PixelBuffer/I420 的格式、附件和灰阶分布，通过 Texture EventChannel 返回统计 | `FlutterRTCVideoRenderer.m`、`rtc_video_renderer_impl.dart` |
| iPad 色彩渲染 | 原生 PixelBuffer 使用 Core Image 按真实 Range/附件转 sRGB BGRA；I420 使用 vImage BT.709 Video-Range 后备 | `FlutterRTCVideoRenderer.m` |
| 诊断界面 | 显示三阶段检查点和 Texture 输入结果；诊断只传统计、不传屏幕像素 | `remote_desktop_panel.dart`、`remote_session_models.dart` |
| Sidecar 切屏事务 | 所有尺寸使用同一 Sender 的 `replaceTrack()`；macOS 视频源根据首帧显式适配真实尺寸；消息携带 generation，控制端等待目标/发送/接收尺寸稳定且不自动二次协商 | `remote_session_controller.dart`、`VideoProcessingAdapter.m`、`remote_session_models.dart` |
| 居中与标题同步 | Texture 和指针共用唯一的居中 `contentRect`；全屏工具栏直接监听会话状态并在切换期间显示进度 | `remote_desktop_panel.dart`、`remote_desktop_geometry_test.dart` |
| 连接生命周期 | WebRTC 瞬时 `Disconnected` 进入 8 秒恢复状态；不关闭 WebSocket/DataChannel、不生成新码，恢复超时或明确离开才终止会话 | `remote_session_controller.dart`、`host_sharing_lifecycle.dart`、对应测试 |
| UI 回归修复 | Host 连接码改用标准 `InputDecorator` + `SelectableText`；工具栏固定图标/宽度；RTCVideoView 使用稳定 Key | `devices_page.dart`、`remote_desktop_panel.dart`、`main_app_test.dart` |
| Sidecar 高清采集 | 协议分离逻辑点和采集像素；macOS 14+ 使用 `contentRect × pointPixelScale` 与最佳采集，旧系统使用 CG 像素尺寸后备；发送器优先保持分辨率 | `MainFlutterWindow.swift`、`FlutterScreenCaptureKitCapturer.m`、`remote_session_models.dart` |
| 稳定画布居中 | 全屏工具栏退出视频 Stack；系统/快捷键盘覆盖在视频上，不再改变画布或触控变换 | `remote_desktop_panel.dart`、`remote_desktop_geometry.dart` |
| 设备页信息降噪 | 推荐地址、连接凭证、状态与操作成为主信息；回环地址、全部网卡和 Java 提示由高级设置控制 | `devices_page.dart`、`theme.dart` |
| 会话与设置 | 会话实时诊断、隐私安全元数据历史、清空确认；持久化默认画质、触控、灵敏度、发现、历史策略和高级网络显示 | `sessions/`、`settings/`、`home_shell.dart` |
| 双端提交切屏 | Mac等待目标采集首帧与RTP新帧，iPad等待本次请求后的解码新帧后提交；关键帧仅尽力请求，统计尺寸与Texture几何不再否决切换 | `remote_session_controller.dart`、`FlutterWebRTCPlugin.m`、`CrossDesktopVideoKeyFrame.m` |
| 直接触控一致性 | 输入使用 `renderedDisplayId` 而不是待切换目标；Mac 将绝对坐标限制在最后一个有效逻辑像素 | `remote_session_controller.dart`、`MainFlutterWindow.swift`、`RunnerTests.swift` |
| 连接码两级限流 | 同一邀请 5 次/分钟、同一来源跨邀请 20 次/分钟；新正确码可连接，轮换随机码仍受保护，错误包含 retryAfter | `SignalingRoomRegistry.java`、`SignalingWebSocketHandler.java`、对应测试 |
| 切屏误回滚修复 | 目标采集首帧及新编码/解码关键帧作为成功门禁；不再用可引用旧屏的普通 P/B 帧或尺寸变化判断完成 | `remote_session_controller.dart`、`remote_session_models.dart`、`CrossDesktopVideoKeyFrame.m` |
| 采集流热切换 | Pending `SCStream`取得首个post-barrier有效目标PixelBuffer后提升为Active；Retired等iPad确认后释放或在失败时恢复；RTC Track/Sender不变 | `FlutterScreenCaptureKitCapturer.m`、`FlutterRTCDesktopCapturer.m`、`desktop_capturer_impl.dart` |
| 延迟优化与诊断 | 原生 SDR 帧绕过 Core Image；自动档采用 1080p60→1080p30→720p60→720p30 梯度和升降档滞回；会话页显示区间码率/丢包、带宽、掉帧/冻结、关键帧、NACK/PLI/FIR 与编解码实现 | `remote_quality_adaptation.dart`、`remote_media_stats.dart`、`sessions_page.dart` |
| 采集端负载控制 | 自动档目标长边/FPS 直接更新 SCK configuration，Sender 仅承担码率和不支持平台的后备缩放；macOS H.264 硬编优先 | `FlutterScreenCaptureKitCapturer.m`、`FlutterRTCDesktopCapturer.m`、`FlutterWebRTCPlugin.m` |
| 切屏过渡隔离 | Pending 预热时 Active 继续输出；提升后 Retired 回调不再进入 WebRTC。宽高比变化立即遮罩，同宽高比切换延迟 120ms，目标 Texture 几何原子提交后淡出 | `FlutterScreenCaptureKitCapturer.m`、`remote_desktop_panel.dart`、`remote_desktop_geometry.dart` |
| 双键盘与稳定画布 | 系统键盘负责 IME 且不再缩放全屏视频；原生桥跟踪停靠/浮动几何；快捷小键盘可拖动并提供导航、修饰键和常用组合键；选择写入设置 | `RemoteImeBridge.swift`、`remote_ime_input_adapter.dart`、`remote_desktop_panel.dart`、`app_settings_controller.dart` |
| 文档 | 更新实现、可行性、依赖说明、README和物理验收判读规则 | `README.md`、`实现方案.md`、`可行性分析.md`、`docs/iPad控制Mac色彩验收.md` |

### 验证

- [x] `flutter analyze` 无问题。
- [x] `flutter test`：54 个常规测试通过；1 个既有 FFI 测试按设计跳过。
- [x] 切屏回归修复后 `flutter analyze` 无问题，Host 生命周期/UI/居中定向 13 项测试通过，macOS Debug 与 iOS Simulator Debug 构建成功。
- [x] `flutter build macos --debug` 成功，验证 ScreenCaptureKit/Core Image/Metal 管线编译链接。
- [x] `flutter build ios --simulator --debug` 成功，验证 Core Image/Accelerate/vImage Texture 管线编译链接。
- [x] `flutter run -d 00008103-001674902207001E --debug --no-resident` 完成真机自动签名、构建和安装；`devicectl` 确认版本 `0.1.0 (1)` 并可独立启动。
- [x] iOS 与 macOS `Package.swift` 均通过 `swift package dump-package`。
- [x] 本地 PostgreSQL、Redis、coturn 均健康；Spring Boot `local` 信令服务完成一次启动验证并监听 `8080`，验证会话结束后未作为后台进程保留；最新版 macOS Debug 应用已打开。
- [x] 新增 Sidecar 不上采样、RTP 视频尺寸解析、Sidecar 双轴居中和 Host 连接码复制测试；定向测试与全量测试均通过。
- [x] 本轮 `flutter build macos --debug`、`flutter build ios --simulator --debug` 成功；`flutter run -d 00008103-001674902207001E --debug --no-resident` 完成物理 iPad 自动签名、安装和启动。
- [x] Sidecar 像素、可用画布、会话和设置优化后，`flutter analyze` 无问题，54 个测试通过。
- [x] `flutter build macos --debug` 与 `flutter build ios --debug --no-codesign` 成功，验证 ScreenCaptureKit 实际像素采集和 iPad 新布局均能原生编译。
- [x] 本轮 `flutter analyze` 无问题；`flutter test` 56 项通过、1 项未配置动态库的 FFI 测试按设计跳过。
- [x] macOS Runner 2 个绝对坐标边界测试通过；既有 iOS 原生 IME 4 项测试保持不变，Swift 测试总数 6。
- [x] Java 9 个测试通过，覆盖旧连接码邀请级封锁后使用新正确码、跨连接码来源级封锁和 `retryAfter`。
- [x] 本轮 `flutter build macos --debug` 与 `flutter build ios --debug --no-codesign` 再次成功。
- [x] 单 `SCStream` 和非阻塞画质门禁变更后 `flutter analyze` 无问题；`flutter test` 57 项通过、1 项未配置动态库的 FFI 测试按设计跳过。
- [x] 单流切屏、SDR 快速路径和媒体诊断变更后的 `flutter build macos --debug` 与 `flutter build ios --debug --no-codesign` 成功。
- [x] 本轮 `flutter analyze` 零告警；`flutter test` 58 项通过、1 项未配置动态库的 FFI 测试按设计跳过。
- [x] 完整帧门禁、事件驱动遮罩、原生键盘几何和双键盘变更后的 `flutter build macos --debug`、`flutter build ios --debug --no-codesign` 成功。
- [x] 最终版本再次通过 macOS Debug 和自动签名 iOS Debug 构建；`flutter run --debug --no-resident` 重新部署物理 iPad，随后 `devicectl` 独立启动成功。
- [x] 关键帧切屏、区间媒体统计、SCK 原生尺寸更新和自动画质变更后，`flutter analyze` 零告警；`flutter test` 63 项通过、1 项未配置动态库的 FFI 测试按设计跳过。
- [x] 同一变更通过 `flutter build macos --debug` 与自动签名 `flutter build ios --debug`，包括 Objective-C/VideoToolbox 编码器包装层的编译链接验证。
- [x] `flutter run -d 00008103-001674902207001E --debug --no-resident` 再次完成 Xcode 构建、安装和启动，最新关键帧/自动画质 Debug 包已部署到物理 iPad。
- [ ] 物理 Mac/iPad 的肉眼灰阶、HDR A/B 和GPU开销尚未验证，不能把构建通过等同于偏白问题已物理关闭。

### 阻塞与风险

- 自动测试无法判断真实面板观感；必须按 `docs/iPad控制Mac色彩验收.md` 记录四个检查点并对比同一灰阶画面。
- Mac 每帧增加一个 Core Image GPU 归一化 pass；需在1080p30/60下测量 GPU、功耗和帧率，再决定常驻或仅对 HDR 显示启用。
- Active/Pending/Retired采集事务和首个有效媒体帧双端提交已通过静态分析、单元测试及双平台构建；关键帧计数、RTP尺寸和Texture几何只作为诊断。需真机确认每次generation都有新编码/解码帧，且commit/rollback后只剩一个`SCStream`，Track/PeerConnection/DataChannel保持。
- `.complete`/尺寸门禁与关键帧提交分别隔离采集过渡帧和编码参考帧污染；若真机仍有残影，先查看 SCK `contentRect`/buffer 几何和关键帧统计，再决定是否增加源端裁剪，不能继续叠加 UI 遮罩。
- 自动画质策略已有 2 个坏样本降档、10 个好样本且稳定 15 秒升档的滞回，但真实 Wi-Fi、Sidecar 和 VideoToolbox 负载仍可能影响阈值，需记录 P95 后再调参。
- 全屏画布已从键盘 Insets 中解耦；仍需覆盖 iPad 停靠、浮动、分离键盘和横竖屏，确认控制栏不遮挡系统键盘且画面不缩放。

### 下一工作日

- [ ] 使用已安装到物理 iPad 的最新 Debug，在 Mac HDR 开启和关闭时分别记录四个检查点。
- [ ] 验证 235～255 灰阶、普通白底、渐变、肤色和主屏/扩展屏。
- [ ] 记录1080p30的帧率、GPU和功耗；通过后再验证1080p60。
- [ ] 若②正常但③异常，继续检查 H.264 VUI/VideoToolbox 色彩属性；若四个检查点正常但肉眼异常，检查 Flutter合成和iPad显示设置。
- [ ] 主屏与 Sidecar 全屏往返切换 30 次，确认会话/连接码不变、名称同步、尺寸一致、画面居中且连接码复制正常。
- [ ] 在色彩诊断中确认 Sidecar 的逻辑尺寸约为 `1311×892 pt`，采集像素尺寸明显更高，且发送/接收尺寸不再出现无依据的上采样。
- [ ] 打开/关闭远程键盘，确认画面始终相对实际可用区域居中，四角触控映射保持一致。
- [ ] 分别选择系统完整键盘和快捷小键盘，重启后确认选择保留；验证快捷键、拖动边界以及停靠/浮动/分离系统键盘。
- [ ] 记录切屏请求到遮罩显示、目标首帧和遮罩淡出的时间；快速切换应不出现遮罩，普通切换不应看到旧 Sidecar 底栏或黑色横条。
- [ ] 连续往返切屏 30 次，确认每次 `keyFramesEncoded` 与 `keyFramesDecoded` 至少各增加 1，横向残影为 0，且不重建 PeerConnection/Track/Sender/DataChannel。
- [ ] 在稳定局域网和限速/丢包环境分别观察自动档：坏样本连续 2 秒应逐级降档，恢复后至少稳定 15 秒才升档，期间无频繁震荡和长时间 Loading。
- [ ] 对比 1080p30/7 Mbps 与 1080p60/12 Mbps 的实际 FPS、P95 RTT、掉帧、冻结、编码耗时和 GPU；若 VideoToolbox 未启用或编码受限，保持 1080p30 而非强推 60 FPS。
- [ ] 验证设置重启后保留、关闭局域网发现立即停止浏览、会话结束后只生成一条不含屏幕/输入内容的历史记录。
- [ ] 使用错误旧码连续失败 5 次，随后让 Mac 生成新码，确认 iPad 无需等待即可连接；再验证大量随机码会触发来源级倒计时。
- [ ] 在适应/填满模式分别点击主屏与 Sidecar 九宫格，确认中心误差不超过 3 个 Mac 逻辑像素、边缘误差不超过 0.5%。

## 11. 2026-08-25 每日记录

### 今日计划

- [x] 提交关键帧切屏、自动画质、输入/UI 与诊断变更。
- [x] 定位主屏/Sidecar 切换期间的画面尺寸抖动。
- [x] 实现切屏期间稳定画布与目标几何原子提交。
- [x] 修复切回主屏后有效画面内缩与直接触控坐标不一致。
- [x] 增加宽高比与画布锁定测试，并完成双平台构建。
- [ ] 在物理 iPad 上完成主屏/Sidecar 往返切换验收。

### 实际完成

| 完成项 | 结果 | 证据 |
| --- | --- | --- |
| 阶段提交 | 创建 `8871ed2 Improve display switching and adaptive streaming`；排除本机 Xcode scheme 状态 | `git show --stat 8871ed2` |
| 画布几何锁定 | 切屏期间继续使用上一幅稳定 `sourceSize/contentRect`，不跟随解码器过渡尺寸反复重排 | `remote_desktop_panel.dart`、`remote_desktop_geometry.dart` |
| 条件遮罩 | 宽高比变化立即显示完全不透明遮罩；同宽高比快速切换保留 120ms 延迟，800ms 后显示等待状态 | `remote_desktop_panel.dart` |
| 原子提交 | 控制器收到本次请求后的新解码媒体帧后一次提交协议画布，Flutter下一帧再淡出遮罩；Texture几何只作后台校验，不再阻塞事务 | `remote_session_controller.dart`、`remote_desktop_panel.dart` |
| 输入隔离 | 整个切屏事务阻止远程指针事件，避免旧坐标变换作用到新显示器 | `remote_desktop_panel.dart` |
| 原生应用结果 | 项目内 `flutter_webrtc` 的切屏/格式更新返回实际 source、generation、宽高和帧率；目标帧放行前预配置 `RTCVideoSource` 输出格式 | `desktop_capturer.dart`、`FlutterScreenCaptureKitCapturer.m`、`VideoProcessingAdapter.m` |
| 媒体流门禁 | 编码端与解码端只要求本次请求后的帧计数前进；关键帧请求是尽力优化，宽高比与Texture尺寸转为后台诊断，不再回滚正常媒体 | `remote_session_controller.dart` |
| 目标流预热 | 不再对已失效的同一 `SCStream` 重发 filter；新建目标流并接收首个post-barrier有效PixelBuffer，超时停止候选且Active不中断 | `FlutterScreenCaptureKitCapturer.m`、`remote_session_controller.dart` |
| 失败回滚与串行化 | 解码/渲染失败期间保留遮罩和旧画布，等待 Mac 恢复原屏；切屏期间阻止手动与自动画质重配置，错误携带阶段和代码 | `remote_session_controller.dart` |
| SCK 有效区域诊断 | 由每帧 `contentRect × scaleFactor` 计算有效像素矩形，记录覆盖率、是否铺满及目标宽高比匹配结果，不再只检查 IOSurface 外层尺寸 | `FlutterScreenCaptureKitCapturer.m`、`remote_session_models.dart` |
| 规范全帧输出 | 有效区域铺满时保留零拷贝；内缩/偏移时在 Core Image/Metal 中裁剪并缩放到目标 Rec.709 Video-Range NV12 全画布，规范化失败则丢帧 | `FlutterScreenCaptureKitCapturer.m` |
| 单一呈现几何 | 已提交的目标显示器尺寸优先于解码器过渡尺寸，外层统一负责 contain/cover、居中、裁剪和直接触控，内层 Texture 只铺满目标矩形 | `remote_desktop_geometry.dart`、`remote_desktop_panel.dart` |
| 几何回归测试 | 覆盖 1920×1080 主屏、Sidecar 往返、异常 1920×832 解码尺寸、适应/填满和指针映射 | `remote_desktop_geometry_test.dart`、`remote_session_models_test.dart` |
| 候选流门禁 | 新流创建后以 `displayTime` 屏障过滤旧回调；`.started/.complete/.idle`有效PixelBuffer、流身份、generation和目标Buffer尺寸（允许2像素对齐误差）是硬条件，异常`contentRect`立即回退规范Buffer | `FlutterScreenCaptureKitCapturer.m` |
| 聚合失败诊断 | Mac 聚合旧帧、错误尺寸、缺失元数据、宽高比和规范化失败次数，Flutter 超时时展示最后拒绝原因，不记录屏幕像素 | `FlutterScreenCaptureKitCapturer.m`、`MainFlutterWindow.swift`、`mac_input_bridge.dart` |
| 两阶段提交/回滚 | iPad确认本次请求后的新解码帧后commit并释放Retired；8秒无新媒体或媒体失败时rollback到未被修改的旧流；缓存一张编码就绪旧帧保证静态桌面也能立即恢复 | `remote_session_controller.dart`、`FlutterScreenCaptureKitCapturer.m` |
| 采集事务串行化 | 切屏先冻结自动档并等待在途格式更新；原生格式回调仅在 stream、source、capture generation 与 format epoch 全部匹配时提交，Pending 不再被画质更新的全局抑帧状态误丢弃 | `remote_session_controller.dart`、`FlutterScreenCaptureKitCapturer.m` |
| 自动画质解耦 | Sender码率/帧率/缩放立即变化，会话内不再动态更新SCK采集尺寸；切屏期间画质请求延后合并，避免格式更新与显示器事务竞争 | `remote_session_controller.dart` |
| Sidecar 居中标准化 | 合法 `contentRect × scaleFactor` 不再受编码目标宽高比门限影响；Mac 裁剪实际内容后等比缩放并居中合成到目标 Rec.709 NV12 画布，避免把非对称空白传至 iPad | `FlutterScreenCaptureKitCapturer.m` |
| Pending首帧门禁 | 目标流以post-barrier首个有效PixelBuffer和精确Buffer尺寸判断就绪；`contentRect`缺失/比例异常立即回退完整画布，最长等待5秒，错误包含几何、内容比例和元数据回退计数 | `FlutterScreenCaptureKitCapturer.m` |
| 四层显示几何协议 | `frameGeometry` 明确携带显示器/代次、逻辑尺寸、采集尺寸、编码尺寸和有效内容矩形；控制端只提交属于本次事务的完整几何 | `remote_session_models.dart`、`remote_session_controller.dart` |
| 画面与输入同源 | 画面 contain/cover 和直接触控统一使用已提交的有效内容矩形，触控归一化不再读取 Texture 的过渡尺寸 | `remote_desktop_geometry.dart`、`remote_desktop_panel.dart` |
| 画质与采集解耦 | 会话建立时固定 1920 长边/60 FPS 采集上限；自动和手动画质只修改 Sender 码率、帧率和缩放，切屏期间的画质请求采用 last-write-wins 并在事务结束后应用 | `remote_session_controller.dart` |
| 内容元数据降级 | `contentRect`匹配时用于裁剪；Sidecar保留旧元数据时立即使用目标流规范Buffer，避免静态桌面的唯一首帧被拒绝 | `FlutterScreenCaptureKitCapturer.m` |
| 无内部黑边规范化 | Mac 对有效内容执行 aspect-fill 到规范编码画布，只允许裁去对齐产生的极小边缘，不再把采集端黑边编码给 iPad | `FlutterScreenCaptureKitCapturer.m` |
| 协议布尔兼容 | 原生MethodChannel或JSON中的`bool`、`NSNumber 0/1`统一在入站边界解析，避免色彩/几何诊断中断控制消息处理 | `wire_value_parsers.dart`、`remote_session_models.dart`、`mac_input_bridge.dart` |

### 验证

- [x] `flutter analyze` 零告警。
- [x] `flutter test` 73 项通过；1 项未配置动态库的 FFI 测试按设计跳过。
- [x] 画布锁定、原子提交、同宽高比缩放与异宽高比切换 7 项定向测试通过。
- [x] 新增目标宽高比拒绝旧关键帧、不同帧计数稳定门禁和原生实际配置解析测试；相关定向测试 18 项通过。
- [x] `flutter build macos --debug` 成功。
- [x] `flutter build ios --debug --no-codesign` 成功。
- [x] `flutter run -d 00008103-001674902207001E --debug --no-resident` 完成自动签名、安装和启动，Active/Pending/Retired 热切换与静态旧帧回滚保护版已部署到物理 iPad。
- [x] 采集事务、自动档分层和 Sidecar 居中修复后再次通过 `flutter analyze`、73 项测试、macOS Debug 与 iOS Device Debug 无签名构建。
- [x] `flutter run -d macos --debug --no-resident` 完成构建并启动同一工作区版本的 Mac 被控端。
- [x] 四层几何、有效内容触控映射和消息解析新增回归后，`flutter test` 75 项通过；1 项未配置动态库的 FFI 测试按设计跳过。
- [x] 本轮 `flutter analyze` 零告警，`flutter build macos --debug` 与 `flutter build ios --debug --no-codesign` 均成功。
- [x] 客户端业务代码不再调用 `updateCaptureFormat`；清晰度切换与显示器事务在采集层解耦。
- [x] `contentRect`硬门禁回归修正后，`flutter analyze`零告警、`flutter test` 78项通过，1项未配置动态库的FFI测试按设计跳过。
- [x] 原生`bool/0/1`协议兼容覆盖3项新增测试；macOS Debug与iOS Device Debug无签名构建均成功。
- [x] 首个有效PixelBuffer/新媒体帧门禁版本已通过自动签名安装并启动到物理iPad，同时在Mac打开同一工作区Debug版本；两端均使用Metal Impeller，iOS部署与macOS启动命令正常退出。
- [x] 本轮修复后`flutter analyze`零告警、`flutter test` 78项通过且1项FFI按环境跳过，macOS Debug与iOS Device Debug无签名构建均成功。
- [ ] 真机仍需确认切回主屏后有效画面铺满稳定画布、黑边恢复预期比例且直接触控九宫格对齐；构建与部署通过不能替代视觉验收。

### 下一步

- [ ] 主屏与 Sidecar 在自动、720p、1080p、2K 四档各往返切换，合计 50 次；成功率 100%，无 `CAPTURE_SWITCH_FAILED`，失败时原屏继续同步且遮罩自动退出。
- [ ] 每次切屏后画面在可用区域内水平/垂直居中，上下或左右对称黑边差不超过 2 个 iPad 物理像素，画面内部无采集黑边。
- [ ] 在日志中确认同一时刻没有 format update 与 display transaction，过期 epoch 回调被忽略，Pending 失败时可看到完整门禁计数。
- [ ] 适应屏幕与填满屏幕模式各测试 10 次；使用九宫格确认中心误差不超过 3 个 Mac 逻辑像素、边缘误差不超过 0.5%，切屏后无需切换清晰度即可保持对齐。
- [ ] 自动档连续运行 10 分钟并主动切换画质 20 次，确认清晰度改变不创建/更新 `SCStream`，无累计性切屏失败，画面恢复不超过 2 秒。
- [ ] 对任何切换失败记录阶段错误码；确认原屏可以自动恢复、画面继续同步且遮罩自动退出。
- [ ] 在 Instruments 中确认 Pending/Retired 只存在于切屏事务，commit/rollback 后回到单 `SCStream`，30 次切换无持续内存、IOSurface 或 GPU 占用增长。

## 12. 2026-08-25 Windows 控制端修复

### 实际完成

| 完成项 | 结果 | 证据 |
| --- | --- | --- |
| 崩溃定位 | Windows 全屏退出日志无 Dart 异常且进程码为 0，按窗口/Surface 生命周期问题处理 | `flutter run -d windows -v` 日志 |
| 桌面键盘 | 新增物理键状态机；可打印文本不再产生孤立 KeyUp，快捷键和特殊键保持成对，失焦统一释放 | `remote_keyboard_translator.dart` |
| IME 分流 | TextField 获得焦点时外层 Focus 不再截获 Backspace、方向键和 Enter，组合文本只在提交后发送 | `remote_desktop_panel.dart`、`remote_text_input_synchronizer.dart` |
| 协议兼容 | 键盘消息增加可选 `physicalHidUsage` 和 `repeat`，旧 `key` 字段继续保留 | `remote_session_controller.dart` |
| Windows 全屏 | 新增项目内 MethodChannel；使用 Win32 样式和窗口位置切换无边框全屏，不重建 HWND、FlutterView或WebRTC Renderer | `desktop_window_mode.dart`、`flutter_window.cpp`、`win32_window.cpp` |
| 单 Surface 布局 | Windows 全屏在设备页原地展开同一个带 GlobalKey 的远程面板；Windows不再进入移动端全屏路由 | `devices_page.dart`、`home_shell.dart`、`remote_desktop_panel.dart` |
| 验收文档 | 增加Windows键盘、微软拼音、30次全屏和iPad回归清单 | `docs/Windows控制Mac验收.md` |

### 验证

- [x] `flutter analyze` 零告警。
- [x] `flutter test` 81 项通过；1 项未配置动态库的 FFI 测试按设计跳过。
- [x] 新增3项键盘状态机测试，覆盖普通文字无孤立KeyUp、快捷键成对和失焦释放。
- [x] macOS Debug 与 iOS 无签名 Debug 构建通过，确认 Windows 专用分支未破坏 Apple 端编译。
- [ ] Windows C++ Runner 必须在 Windows + MSVC 上重新编译；Mac不能替代此项。
- [ ] Windows→Mac物理键盘、微软拼音和30次全屏往返待真机验收。
- [ ] iPad原有触控、双键盘、全屏和主副屏切换待回归。

### 下一步

- [ ] Windows执行`flutter clean`、`flutter build windows --debug`和`flutter run -d windows -v`。
- [ ] 如果MSVC失败，保留第一条C++编译错误并修正，不跳过原生构建。
- [ ] 按`docs/Windows控制Mac验收.md`完成键盘、IME和全屏验收。
- [ ] 完成iPad回归后，再开始Windows被控端采集与`SendInput`。

## 13. 2026-08-25 Windows 直接 IME 输入

### 实际完成

| 完成项 | 结果 | 证据 |
| --- | --- | --- |
| 透明 IME 代理 | Windows远程画面常驻有真实布局但视觉透明的`EditableText`，单击画面即可直接使用微软拼音 | `remote_desktop_panel.dart` |
| 组合状态机 | 拼音组合和候选键留在Windows，只有最终Unicode提交发送远端；取消组合不产生文本 | `windows_remote_ime_coordinator.dart` |
| 键盘分流 | 组合外快捷键、导航键和功能键继续走可靠物理键通道；已发送按键保证收到KeyUp | `windows_remote_ime_coordinator.dart`、`remote_keyboard_translator.dart` |
| 焦点与候选位置 | 候选窗口跟随最近远程点击位置；失焦取消组合并释放远程按键；全屏和视频几何变化不销毁代理 | `remote_desktop_panel.dart` |
| 平台隔离 | Windows通过`Platform.isWindows`独立启用；iOS原生IME、双键盘、触控和全屏路径未修改 | Flutter完整测试与Apple构建 |
| 兼容后备 | 原“系统完整键盘”保留并标记为兼容文本输入，不再是微软拼音必经入口 | `remote_desktop_panel.dart` |

### 自动验证

- [x] `flutter analyze`零告警。
- [x] `flutter test`共93项通过；1项未配置动态库的FFI测试按设计跳过。
- [x] 新增6项Windows IME状态机测试，覆盖拼音提交、候选键隔离、快捷键分流、KeyUp配对、远端编辑基线重置和取消组合。
- [x] macOS Debug构建通过。
- [x] iOS无签名Debug构建通过，确认Apple端编译未回归。
- [ ] Windows Debug构建与微软拼音真机输入待Windows设备验收。
- [ ] iPad原有拼音、双键盘、触控、全屏和主副屏切换待真机回归。

### 下一步

- [ ] Windows重新执行`flutter clean`、`flutter build windows --debug`和`flutter run -d windows -v`。
- [ ] 按`docs/Windows控制Mac验收.md`完成100句拼音、快捷键、退格、Alt+Tab、全屏和切屏组合验收。
- [ ] 通过Windows和iPad物理验收后，再开始Windows被控端采集与`SendInput`。

## 14. 2026-08-25 Windows 被控端首轮单屏闭环

### 实际完成

| 完成项 | 结果 | 证据 |
| --- | --- | --- |
| 原生能力握手 | Windows Runner公布版本、主机、输入、显示器、Unicode和虚拟桌面能力；Dart协议不兼容时拒绝开始共享 | `windows_host_bridge.cpp`、`windows_input_bridge.dart` |
| 平台适配 | `WindowsHostPlatformAdapter`实现完整被控接口，Windows可主动切换为“共享本机”，但仍默认以控制端启动 | `windows_host_platform_adapter.dart`、`device_capabilities.dart`、`home_shell.dart` |
| 显示与DPI | Win32枚举活动显示器、主屏、物理像素和有效DPI；首轮只发布一个主屏，避免未建立稳定身份映射前误切屏 | `windows_host_bridge.cpp`、`remote_session_controller.dart` |
| 鼠标注入 | 归一化坐标结合目标显示器与虚拟桌面映射到`0～65535`；支持绝对/相对移动、左/右/中键、双击拖动和滚轮 | `windows_host_bridge.cpp` |
| 键盘与文本 | 物理键优先使用USB HID Usage对应扫描码；组合键平衡按下/松开；Unicode使用`KEYEVENTF_UNICODE` | `windows_host_bridge.cpp` |
| 状态释放 | 会话断开和Runner销毁时统一释放合成按键、修饰键和鼠标按钮 | `remote_session_controller.dart`、`windows_host_bridge.cpp` |
| 安全边界 | 明确提示UIPI限制：普通权限应用不能控制管理员/UAC安全桌面，不将整个Flutter应用长期提权 | `windows_host_platform_adapter.dart`、`docs/Windows被控端验收.md` |

### 自动验证

- [x] `flutter analyze`零告警。
- [x] `flutter test`共95项通过；1项未配置动态库的FFI测试按设计跳过。
- [x] 新增Windows能力兼容/拒绝、显示器映射及输入MethodChannel契约测试。
- [x] `flutter build macos --debug`与`flutter build ios --debug --no-codesign`通过，Apple端编译无回归。
- [x] Windows首次MSVC构建已进入Runner编译阶段，并定位`/WX`下`VK_* int`隐式缩窄为`WORD`的C4244；键码容器改用`int/UINT`，只在`KEYBDINPUT.wScan`边界显式转换，不关闭警告门禁。
- [ ] Windows C++ Runner只能在Windows 11 + MSVC/Windows SDK上完成编译验证；macOS不能替代。
- [ ] Mac→Windows画面、鼠标、键盘、中文、快捷键、断线释放和30分钟稳定性待物理设备验收。

### 当前边界与下一步

- Windows首轮仅有人值守单主屏；多显示器、DNS-SD、管理员/UAC桌面和无人值守服务未开放。
- Windows MSVC Debug构建和单屏闭环通过后，实现“保留旧流→预热目标流→目标首帧→同Sender `replaceTrack()`→控制端提交几何→释放旧流”的多显示器事务。
- 多显示器通过后接入Windows DNS-SD注册/浏览，并永久保留手动地址后备。

## 15. 2026-08-25 连接租约、Mac直输与Windows自动发现

### 实际完成

| 完成项 | 结果 | 证据 |
| --- | --- | --- |
| 连接码租约 | Java `ready`向Host返回剩余租约，避免客户端/服务端时钟偏差；Host等待期间显示倒计时，过期前或手动操作时生成不同连接码并重新注册 | `SignalingRoomRegistry.java`、`SignalingWebSocketHandler.java`、`host_invitation_lease_controller.dart` |
| 桌面IME复用 | 将Windows专用组合状态机提升为Windows/macOS桌面输入代理；Mac系统拼音完成候选提交后通过现有Unicode文本协议发送Windows | `windows_remote_ime_coordinator.dart`、`remote_desktop_panel.dart` |
| iPad隔离 | 桌面IME只在Windows/macOS启用，iOS继续使用UIKit原生IME、双键盘与移动端全屏路径 | 平台分支与Flutter全量测试 |
| Windows DNS-SD | Runner接入`DnsServiceRegister`、`DnsServiceBrowse`、`DnsServiceResolve`和异步注销；注册/解析资源按WinDNS所有权释放，Flutter事件仅在窗口线程提交 | `windows_lan_discovery_bridge.cpp/.h`、`flutter_window.cpp`、`CMakeLists.txt` |
| 安全边界 | TXT只发布设备ID、协议版本、信令路径和能力，不发布连接码；发现失败永久保留手动地址 | `lan_discovery_service.dart`、`docs/局域网发现与跨平台适配.md` |

### 自动验证

- [x] `flutter analyze`零告警。
- [x] `flutter test`共99项通过，1项未配置动态库的FFI测试按设计跳过。
- [x] Java `./gradlew test`通过。
- [x] 新增连接租约、桌面IME和不支持平台发现行为测试。
- [ ] Windows Runner新增的WinDNS C++只能在Windows 11 + MSVC/Windows SDK上完成编译验证。
- [ ] Mac系统拼音→Windows、Windows↔Mac互相发现及网络/睡眠恢复待物理设备验收。

### 下一步

- [ ] Windows执行`flutter clean`、`flutter build windows --debug`，以MSVC首条错误为准修复且不关闭`/WX`。
- [ ] Windows与Mac同网段互相开始共享，验证发现/消失均不超过5秒，失败时手动地址仍正常。
- [ ] Mac直接输入100句系统拼音，覆盖候选翻页、退格、取消组合、快捷键和应用失焦恢复。
- [ ] Host等待超过5分钟并手动刷新30次，确认连接码确实变化、旧码失效、新码可立即连接且共享意图不丢失。

## 16. 2026-08-26 桌面高帧率、远端输入法、连接码与发现边界

### 实际完成

| 完成项 | 结果 | 证据 |
| --- | --- | --- |
| 桌面画质档位 | 新增1080p60、2K60、原画60；高帧率档仅桌面控制端可选，Apple采集上限保持不变，Windows单屏采集上限提升到2560长边/60 FPS | `remote_session_models.dart`、`remote_session_controller.dart`、`remote_quality_adaptation.dart` |
| Windows特殊键 | Enter、Backspace、导航和功能键使用跨平台语义名称，由Windows映射到稳定虚拟键；可打印按键仍以USB HID驱动被控端IME | `remote_keyboard_translator.dart`、`windows_host_bridge.cpp` |
| 被控端输入法入口 | Mac/Windows控制Windows时，远程工具栏直接提供“本机输入法/被控端输入法”，并提示在被控Windows使用`Win+Space`选择微软拼音 | `remote_desktop_panel.dart` |
| 一次性连接码 | 连接码消费后即使媒体建立前失败也会重新等待并生成新码；活动会话显示“当前码已使用”，禁止破坏当前路由的中途轮换 | `host_sharing_lifecycle.dart`、`devices_page.dart` |
| 发现与信令边界 | DNS-SD继续发现被控设备；用户选中设备后，空/回环/不同配置会采用设备声明的rendezvous URL；UI明确独立公网信令不属于mDNS服务器目录 | `lan_discovery_service.dart`、`devices_page.dart`、`docs/局域网发现与跨平台适配.md` |

### 自动验证

- [x] `flutter analyze`零告警。
- [x] `flutter test`共108项通过；1项未配置动态库的FFI测试按设计跳过。
- [x] 新增画质档位、语义特殊键、连接码消费前失败恢复和信令地址选择测试。
- [ ] Windows MSVC构建、高分辨率实际FPS、微软拼音、断开自动换码和Mac↔Windows mDNS仍需物理设备验收。
- [x] `flutter build macos --debug`与`flutter build ios --debug --no-codesign`通过，确认本轮桌面分支未破坏Apple构建。

### 当前边界与下一步

- 当前稳定上限设为60 FPS。120 FPS需要证明采集、硬件编码、网络、解码和Flutter呈现五段都能持续达到目标，不能只增加下拉选项。
- 被控端输入法模式依赖Windows当前前台输入法和`SendInput`，不会绕过UIPI，也不能控制UAC安全桌面。
- Windows执行Debug构建后，分别验证1080p60与2K60持续10分钟的实际发送/接收FPS、掉帧、冻结和编码耗时；不达标时回退到1080p60，不伪装为2K60。
- Mac→Windows覆盖微软拼音、Enter、Backspace、方向键、快捷键和失焦释放；再完成连接20次、断开自动换码20次以及双向发现/消失5秒门禁。

## 17. 2026-08-26 桌面双工发现、副屏几何与会话修复

### 今日计划

- [x] 解除局域网浏览与控制端角色绑定，使Mac/Windows共享时也可浏览其他设备。
- [x] 修复Windows控制Mac Sidecar时RTC视图二次缩放导致的画面偏小与指针偏移。
- [x] 将工具栏刷新从“仅刷新显示器列表”升级为不重连的当前画面/控制修复。
- [x] 将连接码生成、轮换文案与屏幕采集语义解耦。
- [x] 完成Flutter测试和Apple构建回归。

### 实际完成

| 完成项 | 结果 | 证据 |
| --- | --- | --- |
| 桌面双工发现 | 设备页在控制/共享角色均启动浏览；共享上线后发布与浏览并行，本机条目按设备ID过滤，多网卡重复结果去重 | `lan_discovery_service.dart`、`devices_page.dart` |
| Windows发现诊断 | Runner提供浏览、发布、已解析、解析中和最近WinDNS错误；空列表给出专用网络/防火墙/mDNS边界提示 | `windows_lan_discovery_bridge.cpp/.h`、`lan_discovery_service.dart` |
| 信令/采集解耦 | “上线等待连接”只建立信令并发布DNS-SD；验证连接码后才启动采集。空闲可直接生成连接码，等待时可轮换，活动会话中保持当前单次码，断开后自动补发 | `devices_page.dart`、既有`host_sharing_lifecycle.dart` |
| Windows单一呈现几何 | Windows控制端绕过`RTCVideoView`内部二次fit，直接绘制原生Texture；可见裁剪、居中和指针归一化共用`RemoteContentTransform`。Apple路径不变 | `remote_desktop_geometry.dart`、`remote_desktop_panel.dart` |
| 原地会话修复 | 刷新按钮请求被控端释放残留输入、复查权限、同步显示器/画质并请求关键帧；桌面端重绑现有纹理，不重建信令、PeerConnection、Track、Sender或DataChannel | `remote_session_controller.dart`、`remote_desktop_panel.dart` |
| UI语义 | 主刷新按钮修复当前画面和控制；显示器列表刷新移入显示器菜单；发现与信令服务器的边界保持明确 | `remote_desktop_panel.dart`、`devices_page.dart` |

### 自动验证

- [x] `flutter analyze`零告警。
- [x] `flutter test`共112项通过；1项未配置Rust动态库的FFI测试按设计跳过。
- [x] 新增DNS-SD双工去重/诊断和WindowsTexture裁剪几何回归测试。
- [x] `flutter build macos --debug`成功。
- [x] `flutter build ios --debug --no-codesign`成功，确认iPad编译路径未回归。
- [ ] Windows WinDNS/C++和原生Texture必须在Windows 11 + MSVC上构建和物理验收；macOS不能替代。

### 阻塞与风险

- Windows防火墙或公共网络策略可能阻止UDP 5353；应用只诊断并保留手动地址，不擅自修改系统防火墙。
- Windows原生Texture分支已通过Dart几何测试和Apple交叉构建，但Sidecar视觉比例、清晰度和九宫格点击仍需Windows设备确认。

### 下一工作日

- [ ] Windows执行`flutter clean`、`flutter build windows --debug`，确认新增WinDNS诊断在`/WX`下零警告。
- [ ] Mac与Windows分别上线等待，验证互相发现/消失不超过5秒；Windows空列表时记录发现诊断文本。
- [ ] Windows控制Mac主屏/Sidecar往返30次，适应/填满各验证九宫格，中心误差不超过3个逻辑像素、边缘误差不超过0.5%。
- [ ] 制造静态画面、控制无响应和显示列表变化，验证刷新在3秒左右恢复且连接码、PeerConnection和DataChannel不变。
- [ ] iPad回归局域网发现、主副屏、直接触控和全屏键盘，确认平台隔离成立。

## 18. 2026-08-26 Windows 副屏活动区域、连接码在线化与 DNS-SD 修复

### 今日计划

- [x] 将 Sidecar 的真实活动内容区域从 ScreenCaptureKit 传递到控制端。
- [x] 让 Windows 显示裁剪与直接触控使用同一份活动区域几何。
- [x] 将设备在线、连接码、媒体采集三个生命周期拆开。
- [x] 修复 Windows DNS-SD 发布时未绑定可用 IPv4 地址的问题。
- [x] 增加平台能力协商，保持 iPad 使用既有稳定路径。

### 实际完成

| 完成项 | 结果 | 证据 |
| --- | --- | --- |
| 活动内容几何 v2 | Mac 采集端保留稳定编码画布，同时上报真实 `activeContentRect`；Windows 控制端裁去画布留黑后按长边最大化适应 | `FlutterScreenCaptureKitCapturer.m`、`remote_session_controller.dart` |
| 显示/触控同源 | Windows 画面布局与直接触控共同使用 `RemoteContentTransform` 和同一活动区域，避免下半区误差随距离放大 | `host_platform_adapter.dart`、`remote_desktop_geometry.dart` |
| Apple 路径隔离 | 仅 Windows 控制端通过信令声明 `active-content-geometry-v2`；iPad 不声明能力，继续使用已验收的 Apple 兼容归一化路径 | `signaling_endpoint.dart`、`SignalingWebSocketHandler.java` |
| 在线与采集解耦 | 进入共享角色后自动连接信令并取得连接码，但验证通过前不启动屏幕采集；控制端断开后清空输入码，被控端自动重新上线取得新码 | `devices_page.dart`、`host_sharing_lifecycle.dart` |
| Windows DNS-SD 地址修复 | 发布时枚举已启用且支持组播的 IPv4 接口，并把地址显式交给 `DnsServiceConstructInstance`；新增浏览、PTR、解析和注册计数诊断 | `windows_lan_discovery_bridge.cpp/.h`、`lan_discovery_service.dart` |
| 发现设置闭环 | “局域网发现”同时控制浏览与主机发布；关闭后停止两者，手动信令地址永久保留 | `devices_page.dart` |

### 自动验证

- [x] `flutter analyze` 零告警。
- [x] `flutter test` 共 113 项通过；1 项未配置 Rust 动态库的 FFI 测试按设计跳过。
- [x] Java `./gradlew test` 通过，新增控制端平台/能力在采集前送达 Host 的集成测试。
- [x] `flutter build macos --debug` 成功。
- [x] `flutter build ios --debug --no-codesign` 成功，确认 iPad 编译路径未回归。
- [ ] Windows Runner 新增的 WinDNS、IP Helper 和 Winsock 代码必须在 Windows 11 + MSVC 下完成构建和物理验收。

### 物理验收

- [ ] Windows 控制 Mac 主屏/Sidecar 往返 30 次；每次长边尽可能占满、短边保持比例，无二次缩小。
- [ ] Sidecar 九宫格直接点击；中心误差不超过 3 个逻辑像素，边缘误差不超过 0.5%。
- [ ] Mac/Windows 启动应用并进入共享角色后，无需开始采集即可看到连接码；断开后控制端码清空、被控端 3 秒内取得不同新码。
- [ ] Mac 与 Windows 在同一私有网络互相发现/消失不超过 5 秒；Windows 诊断中注册地址、PTR 与解析成功计数可见。
- [ ] iPad 回归主副屏、直接触控、全屏和局域网发现，确认行为与本轮修改前一致。

## 19. 2026-08-27 桌面双击与 Windows 窗口/采集恢复

### 今日计划

- [x] 修复 Windows 控制 Mac 时桌面物理鼠标双击无效。
- [x] 修复 Windows 被控应用最小化、最大化和改尺寸后 Flutter Surface 不再出帧。
- [x] 为 Windows 不支持显式关键帧请求的采集链路增加同 Sender 热重启兜底。
- [x] 保持 iPad 手势、信令、连接码和 DataChannel 路径不变。

### 实际完成

| 完成项 | 结果 | 证据 |
| --- | --- | --- |
| 系统双击 | Windows 控制端读取系统双击时间和 DPI 换算后的范围；第一次点击立即发送，第二次真实 down/up 使用 `clickCount=2` | `desktop_window_mode.dart`、`desktop_mouse_click_tracker.dart` |
| Windows 输入语义 | Windows 被控端不再为 `clickCount=2`额外伪造一组点击，避免真实双击被解释为三击 | `windows_host_bridge.cpp` |
| Surface 恢复 | 最小化不再把 Flutter 子窗口永久改成`0×0`；恢复、最大化、调整尺寸和显示器变化执行立即+50ms延迟重绘 | `win32_window.cpp`、`flutter_window.cpp` |
| 采集监督 | Windows 窗口变化后采样`framesEncoded`；650ms未推进时，仅预热当前显示器新轨道并在同一 Sender `replaceTrack()`，成功后释放旧流，失败回滚 | `remote_session_controller.dart` |
| 手动修复 | Windows“刷新当前画面”不再依赖未实现的关键帧接口，改用同一采集热重启；不重连信令、不刷新连接码 | `remote_session_controller.dart` |
| 平台隔离 | iPad 的`RemoteTouchGestureController`、UIKit IME、移动全屏和 Mac ScreenCaptureKit 切屏事务均未修改 | 平台分支与完整 Flutter 测试 |

### 自动验证

- [x] `flutter analyze`零告警。
- [x] `flutter test`共115项通过；1项未配置Rust动态库的FFI测试按设计跳过。
- [x] 新增桌面双击、拖动/超时/按键/显示器切断点击链测试。
- [ ] Windows Runner 的 `GetDoubleClickTime`、窗口消息和重绘代码需在 Windows 11 + MSVC `/WX` 下构建验证。
- [ ] 双击30次及窗口状态各30次物理验收待Windows设备执行。

### 物理验收

- [ ] Windows→Mac标题栏、Finder文件和文本单词双击各30次，成功率100%，单击/右键/拖动无回归。
- [ ] Mac→Windows最小化/恢复、最大化/还原、改尺寸各30次，无永久冻结且无需本机移动鼠标。
- [ ] 自动恢复目标约800ms；发生采集热重启时会话、连接码、Sender与DataChannel保持不变。
- [ ] iPad回归触控双击、拖选、右键、主副屏和全屏键盘。

## 20. 2026-08-27 文件与剪贴板阶段 1

### 今日计划

- [x] 新增版本化剪贴板与文件传输 Protobuf。
- [x] 实现共同能力协商和旧客户端安全降级。
- [x] 完成 Rust 传输状态机、限额、路径/Manifest 校验、恢复位图和 SHA-256。
- [x] 扩展窄 C ABI 任务控制、进度订阅和 WebRTC 原生回调适配。
- [x] 完成协议生成、Rust/Java/Dart 验证和文档同步。

### 实际完成

| 完成项 | 结果 | 证据 |
| --- | --- | --- |
| 数据协议 | `clipboard.proto`采用格式清单与按需获取；`transfer.proto`分离元数据、控制、恢复位图和文件块 | `proto/crossdesktop/v1/clipboard.proto`、`transfer.proto` |
| 兼容协商 | 主版本不兼容或旧端缺少字段时仅关闭数据能力；格式、限额和WebRTC传输取双方交集 | `session-core/src/lib.rs`及兼容测试 |
| 传输核心 | 状态机、总量/单文件/条目/路径/16KiB线速分片限额、Manifest、符号链接预检、256KiB恢复位图和流式SHA-256 | `crates/transfer-core/src/*` |
| C ABI | 不透明Manager支持创建、接受、暂停、恢复、取消、校验完成、进度查询/订阅；WebRTC回调检查消息大小和`bufferedAmount` | `client-ffi/src/transfer.rs`、`include/crossdesktop_core.h` |
| 传输抽象 | `TransferTransport`隔离任务与数据面，V1实现原生回调式WebRTC，保留QUIC实现位置 | `transfer-core/src/transport.rs`、`docs/文件与剪贴板传输协议.md` |

### 验证

- [x] `cargo fmt --all -- --check`通过。
- [x] `cargo clippy --workspace --all-targets -- -D warnings`通过。
- [x] `cargo test --workspace`共27项通过。
- [x] `buf lint proto`与`protoc`描述符生成通过。
- [x] `scripts/generate-proto.sh`成功生成Java、Dart、Rust绑定；Dart生成代码`analyze`零问题。
- [x] Java控制平面`gradlew test`通过。
- [ ] 原生WebRTC DataChannel与平台文件/剪贴板尚未接入，本阶段不做端到端文件传输声明。

### 风险与下一步

- 路径预检不能消除TOCTOU；平台写入器必须使用目录相对、no-follow方式创建随机`.cdrpart`文件并在SHA-256通过后原子重命名。
- 下一阶段先接原生`clipboard`、`transfer-control`、`file-n` DataChannel，再接Windows/macOS文件IO与文本剪贴板；Flutter只消费任务命令和进度DTO。
- 需要补充协议breaking基线、属性/模糊测试、断线恢复、磁盘不足和背压互操作测试。

## 21. 2026-08-27 Mac ↔ Windows 文本剪贴板

### 今日计划

- [x] 接入 macOS/Windows 剪贴板监听与 UTF-8 文本写入。
- [x] 完成双向、两种单向和关闭策略。
- [x] 实现 revision、SHA-256、防重放和原生写入回声抑制。
- [x] 增加剪贴板状态、方向控制和本机持久化。
- [x] 保持 iPad/旧客户端降级路径不变。

### 实际完成

| 完成项 | 结果 | 证据 |
| --- | --- | --- |
| 平台剪贴板 | macOS 使用 `NSPasteboard.changeCount` 250ms 轮询；Windows 使用 `AddClipboardFormatListener`/`WM_CLIPBOARDUPDATE`；两端均使用 `CF_UNICODETEXT`/UTF-8 边界 | `MainFlutterWindow.swift`、`windows_clipboard_bridge.*` |
| 会话兼容 | 仅双方声明 `text-clipboard-v1` 时创建独立 ordered/reliable `clipboard` DataChannel；未知通道不再覆盖 control | `remote_session_controller.dart` |
| 安全传输 | 只同步连接后新复制的文本；256 KiB 上限；9 KiB 分片；offset/大小/SHA-256 校验 | `text_clipboard_sync_engine.dart` |
| 回环抑制 | 本地 OS revision 去重、远程 revision 防重放、hash 匹配的原生写入回声只抑制一次 | 剪贴板状态机及单测 |
| 策略与 UI | 关闭/双向/控制端→被控端/被控端→控制端；设置页显示实时状态并持久化 | `clipboard_sync_mode.dart`、`settings_page.dart` |
| 平台隔离 | iPad/iPhone 不声明能力、不创建剪贴板通道、不显示桌面剪贴板设置 | capability 门禁与 iOS 构建 |

### 自动验证

- [x] `flutter analyze` 零告警。
- [x] Flutter 全量 124 项测试通过，1 项未配置 Rust 动态库的 FFI 测试按设计跳过；剪贴板新增 10 项专项测试通过。
- [x] `flutter build macos --debug` 成功，确认 Swift 原生桥可编译。
- [x] `flutter build ios --debug --no-codesign` 成功，确认 iPad/iOS 路径未回归。
- [ ] Windows C++ 桥需在 Windows 11 + MSVC `/WX` 下构建验证。
- [ ] Mac ↔ Windows 双机系统剪贴板物理验收待执行。

### 物理验收

- [ ] 两个方向分别连续复制中文、英文、emoji、多行和空文本 30 次，每次仅同步一次。
- [ ] 双向、两种单向、关闭热切换，禁止方向无任何系统剪贴板写入。
- [ ] 256 KiB 内容完整通过；超限内容仅提示且不断开会话。
- [ ] 连接前剪贴板不同步；断线后不同步；重连后只同步新复制的内容。
- [ ] 与未声明 `text-clipboard-v1` 的旧客户端连接，视频、鼠标和键盘仍正常。

## 22. 2026-08-28 跨平台剪贴快捷键与切屏几何防回归

### 今日计划

- [x] 核对 `02f37f6` 与文本剪贴板版本的渲染/指针差异。
- [x] 统一 Windows、macOS 和 iPadOS 主快捷键语义。
- [x] 增加剪贴板原生写入确认和显式粘贴顺序。
- [x] 拒绝往返切屏后延迟到达的旧 generation 几何。

### 实际完成

| 完成项 | 结果 | 证据 |
| --- | --- | --- |
| 回归定位 | 剪贴板版本未修改渲染、指针和采集文件；定位到同一显示器ID往返时旧画质消息可覆盖新几何 | `remote_session_controller.dart` generation 门禁 |
| 快捷键策略 | Windows `Ctrl` 与 macOS/iPadOS `Command` 按被控系统转为主快捷键；只接管 `A/C/F/V/X/Z` | `remote_shortcut_policy.dart` 及单测 |
| 小键盘 | 常用快捷键按被控端动态显示 `⌘`/`Ctrl`，原始 Command 键在 Windows 主机显示为 `Win` | `remote_desktop_panel.dart` |
| 粘贴顺序 | 显式粘贴先发布当前剪贴板，新客户端等待 `applied`后再发键；旧客户端无等待降级 | `text_clipboard_sync_engine.dart` |
| 几何原子性 | 画质状态和展示层都要求 `displayId + generation` 匹配，画面与指针继续共用同一 Transform | 控制器、展示层及往返切屏单测 |

### 自动验证

- [x] `flutter analyze` 零告警。
- [x] Flutter 全量 131 项测试通过，1 项未配置 Rust 动态库的 FFI 测试按设计跳过。
- [x] `flutter build macos --debug` 成功。
- [x] `flutter build ios --debug --no-codesign` 成功。
- [ ] Windows Debug 构建与双机物理验收待 Windows 设备执行。

### 物理验收

- [ ] Windows `Ctrl+C/V` 可操作 Mac 应用，Mac `Command+C/V` 可操作 Windows 应用。
- [ ] iPad 物理键盘和快捷小键盘可按被控系统正确复制粘贴。
- [ ] Windows→Mac 主屏→副屏→主屏往返 30 次，无尺寸和指针几何回归。
- [ ] 剪贴板关闭、单向、双向不改变画面尺寸和指针映射。

### 已知风险

- [x] worktree 引用已通过保留 stash、获取远端和 Fast-forward 修复到 `603a266`，本地与远端基线一致。

## 23. 2026-08-28 Windows 控制 Mac 切屏几何恢复

### 今日计划

- [x] 精确比较 `02f37f6` 与当前版本的渲染、指针和切屏提交差异。
- [x] 确认后续 generation 门禁未恢复 `02f37f6` 的物理效果。
- [x] 将显示几何接收和展示判断精确恢复为 `02f37f6`。
- [x] 保留后续快捷键、剪贴板、文件传输和 Windows 构建修复。

### 实际完成

| 完成项 | 结果 | 证据 |
| --- | --- | --- |
| 回归归属 | `603a266`和`a3aea5a`引入的 generation 几何判断改变了已验收行为；仅更换 generation 来源仍未修复真机问题 | `git diff`、Windows真机结果 |
| 精确恢复 | `quality-state`按当前显示器ID接收几何；展示层按有效性和显示器ID消费几何，与`02f37f6`一致 | 控制器、展示层逐段对比 |
| 无新增补偿 | 删除`_committedDisplayGeneration`及相关赋值/门禁，不增加新状态、缩放系数或坐标修正 | 生产代码差异审计 |
| 功能隔离 | 快捷键策略、Clipboard DataChannel、原生剪贴板桥和文件传输核心保持当前实现 | 独立模块及非几何hunk |

### 自动验证

- [x] `flutter analyze` 零告警。
- [x] 几何、快捷键和剪贴板定向测试 26 项通过。
- [x] Flutter 全量 130 项测试通过，1 项未配置 Rust 动态库的 FFI 测试按设计跳过。
- [x] macOS Debug 与 iOS 无签名 Debug 构建通过。
- [ ] Windows→Mac主副屏物理验收待Windows设备执行。

### 物理验收

- [ ] 自动/720p/1080p/2K下主屏↔Sidecar各往返30次，切屏成功或失败均不丢失最后提交几何。
- [ ] 长边最大化、短边居中，仅保留宽高比必需黑边。
- [ ] 九宫格和顶部/中部/底部点击无纵向累积误差，中心误差不超过3逻辑像素。
- [ ] Mac/Windows剪贴板快捷键和iPad控制Mac路径无回归。

## 24. 每日记录模板

复制以下模板到文件末尾，并按日期填写：

```markdown
## YYYY-MM-DD 每日记录

### 今日计划

- [ ] 任务 1
- [ ] 任务 2

### 实际完成

| 完成项 | 结果 | 证据 |
| --- | --- | --- |
|  |  |  |

### 验证

- [ ] 测试/构建/性能结果

### 阻塞与风险

- 无；或写明阻塞原因、所需决策和复查时间。

### 下一工作日

- [ ] 下一项任务
```
