# Cross-project tests

- `interoperability`：协议版本、客户端/服务端和多平台互操作。
- `network-lab`：延迟、丢包、抖动、NAT、TURN 回退实验。
- `performance`：采集、编解码、传输、渲染和输入到画面指标。

原始性能数据可能包含硬件、网络或用户信息，应写入被 `.gitignore` 排除的 `performance-results/raw/`。
