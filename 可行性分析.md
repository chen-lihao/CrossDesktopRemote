# CrossDesktopRemote 可行性分析

> 文档版本：0.2
> 评估基准日：2026-08-17
> 配套文档：[实现方案.md](./实现方案.md)

## 1. 可行性结论

项目 **有条件可行**，但必须调整为分层平台承诺：

- Windows、macOS、Linux 作为被控 PC，Windows/macOS/Linux/iOS/iPadOS/Android 作为控制端：技术可行。
- Android 作为被控端：有人值守、每次取得系统授权的方案可行；普通应用无法稳定承诺与 PC 相同的静默无人值守。
- iPhone/iPad 作为被控端：用户主动屏幕共享可行，第三方通用应用对整个系统进行任意输入控制和无人值守不可作为可交付承诺。
- 1080p60 和 2K60：在现代硬编硬解设备上可行。
- 4K30/60：在双方硬件、显示器、上行带宽和网络质量满足条件时可行，不能默认保证所有设备/网络。
- 4K 高帧率、4:4:4、HDR 和弱网同时满足：属于专业版后续研发，不适合作为 MVP 验收线。
- 原文件传输、剪贴板、多显示器、设备信息、截图、有人/无人值守、会话元数据和本地录像：均有成熟实现路径。
- 安全可靠和良好扩展性：可行，但前提是从第一个原型就采用设备身份、最小权限、协议版本化和高权限进程隔离，不能在功能完成后再补安全。

技术栈已最终收敛为 **Flutter 跨端 UI + Rust 客户端核心 + 平台原生媒体适配 + Java/Spring Boot 控制平面 + PostgreSQL/Redis + 独立 coturn**。不采用 Electron 和 Go。该调整不改变媒体性能架构：Dart 与 Java 均不进入屏幕帧的采集、编解码和 P2P 传输热路径。

以 10–14 人的有经验团队估算：Windows 功能型 MVP 约 3–4 个月；达到可公开使用的 Windows 稳定版约 5–7 个月；覆盖三种 PC 主机和三类移动控制端并达到生产质量，现实周期约 12–18 个月。小于 6 人的团队也能做原型，但不应同时承诺完整跨平台、4K60、企业安全和高可用服务。

## 2. 评估前提

本分析采用以下假设：

1. 产品允许安装被控端 Agent，并由用户/管理员授予操作系统要求的屏幕和辅助功能权限。
2. 首版不要求使用自研视频编解码器，也不要求自研完整 NAT 穿透协议。
3. “跨设备”表示每个平台根据系统能力提供相应角色，不表示所有设备之间完全对称互控。
4. 默认 SaaS 控制平面，同时保留企业私有部署能力；媒体优先 P2P。
5. “原文件传输”表示字节一致、可校验、支持断点续传，而不是把文件通过画面或第三方网盘中转。
6. 性能目标在明确的硬件、网络与内容类型下验收，不采用厂商营销页的单一最低延迟作为交付标准。
7. 项目是否开源、商业模式、首发国家/地区尚未确定；相关许可证、隐私和密码合规结论因此只能给出工程前置项，不能替代正式法律意见。

## 3. 综合评分

| 维度 | 评分（5 分制） | 判断 |
| --- | ---: | --- |
| PC 远控核心技术 | 4.5 | 操作系统、硬编和实时传输都有成熟基础，主要是工程集成与质量问题 |
| 1080p60/2K60 | 4.5 | 现代 PC 与移动设备普遍具备硬解条件，原型可快速验证 |
| 4K60 全覆盖 | 3.0 | 可在能力白名单上实现，受 GPU、编解码器、色彩、带宽和温控限制 |
| 全平台控制端 | 4.0 | Flutter + 原生媒体纹理可行，难点是输入和移动 UX，不是普通页面开发 |
| 全平台被控端 | 2.5 | Android、iOS/iPadOS、Wayland 的安全模型决定能力不对称 |
| 文件/剪贴板/记录 | 4.5 | 协议成熟，重点是正确性、权限和安全边界 |
| 有人/无人值守 | 4.0 | PC 可行；移动端和 Wayland 必须条件化承诺 |
| 安全与企业治理 | 3.5 | 技术可行，工作量容易被低估，需要专门安全投入 |
| 自研与长期扩展 | 4.0 | 分层与能力协商能支持，但过早插件化或微服务化会拖慢首版 |
| 团队与周期风险 | 2.5 | 涉及图形、网络、系统权限、移动端、后端、运维和安全，非普通跨端应用规模 |

综合判断为 **3.7/5：可立项，但必须先做性能与平台权限原型，按门禁逐步扩平台**。

## 4. 竞品信息对可行性的证明

### 4.1 已被市场反复验证的能力

- 向日葵和 ToDesk 已证明国内网络环境下，PC/移动控制、文件、多屏、无人值守、远程设计/游戏可以组成完整消费级产品；两者也展示了 4K、高帧率、真彩和专线/节点加速的商业分层空间。[向日葵功能页](https://sunlogin.oray.com/product/feat?ici=SL_HOME_BANNER)、[ToDesk 功能页](https://www.todesk.com/special.html)
- Parsec 已证明 GPU 零拷贝、硬件编解码、动态码率和面向低延迟优化的 P2P UDP 管线能够支持高交互图形工作；其官方技术说明把采集、编码、网络、解码和渲染作为完整链路处理。[Parsec Overview](https://support.parsec.app/hc/en-us/articles/32361354307348-Overview)
- TeamViewer 已证明远控产品可以在临时支持之外扩展到企业无人值守、可信设备、2FA/SSO、条件访问、连接报告、录像和 SIEM 审计。[TeamViewer Security](https://www.teamviewer.com/en/special/security-explained/)
- RustDesk 已证明 Rust + Flutter、P2P/中继和自托管是一条实际可运行的跨平台架构路线，其官方文档列出 Windows、macOS、Linux、iOS、Android 和 Web 支持。[RustDesk 文档](https://rustdesk.com/docs/en/)

这些产品证明“能做”，但不证明一个新团队能一次性低成本达到相同成熟度。真正门槛是长尾兼容、连接成功率、更新安全、全球中继容量、权限引导和持续质量运营。

### 4.2 可以形成机会的缺口

- Parsec 性能强，但其官方矩阵当前显示主机主要是 Windows/macOS，控制端不支持 iOS/iPadOS，且产品不以完整文件中心为核心。[Parsec 平台兼容性](https://support.parsec.app/hc/en-us/articles/32381568346644-Hardware-and-Software-Compatibility)
- RustDesk 在自托管和透明度上有优势，但专业图形体验、统一移动 UX 与企业精细化体验仍给新产品留下空间。
- 国内产品峰值参数很高，但对延迟口径、不同设备实际能力和弱网降级的公开可验证性有限。CrossDesktopRemote 可用“实时链路统计 + 可重复基准测试”建立信任。
- 企业产品往往复杂和昂贵，个人高性能产品又缺少企业治理；“性能优先 + 安全默认 + 可私有化”的中间位置有产品逻辑。

## 5. 平台可行性

### 5.1 Windows：高可行，首选验证平台

**可实现能力**：多屏采集、硬件编码、键鼠、剪贴板、文件、登录后无人值守、设备信息、截图、系统音频、虚拟显示器和会话录像。

**风险**：

- UAC、登录桌面和高完整性应用不是普通用户进程可以直接控制的范围。微软明确说明 `SendInput` 受 UIPI 限制，只能向相同或更低完整性级别注入输入。[SendInput 文档](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-sendinput)
- 服务运行在 Session 0，而用户桌面位于交互 Session，因此需要“系统服务 + 每用户工作进程”架构，不能让高权限服务直接承担全部 UI 和媒体解析。
- 显卡驱动重置、显示器热插拔、HDR、混合 GPU 笔记本和安全桌面会导致采集链重建。

**结论**：适合作为阶段 0/1 的唯一完整平台；DXGI 能提供 GPU surface、脏区、移动区和指针信息，官方也明确把它用于桌面协作与远程访问。[Desktop Duplication API](https://learn.microsoft.com/en-us/windows/win32/direct3ddxgi/desktop-dup-api)

### 5.2 macOS：可行，权限体验是主要风险

**可实现能力**：ScreenCaptureKit 高性能采集、VideoToolbox 硬编硬解、多显示器、键鼠、剪贴板、文件和 Agent 启动。

**限制**：屏幕录制和 Accessibility 权限必须由用户或受管设备策略授予；首次授权、版本升级、签名/Bundle ID 变化都可能影响权限状态。Apple 官方要求捕获前获得屏幕录制许可。[ScreenCaptureKit](https://developer.apple.com/documentation/ScreenCaptureKit)

**结论**：技术风险中等；开发难点不只是 API，而是签名、公证、权限引导、登录项和系统版本回归。

### 5.3 Linux：可行，但支持矩阵必须收敛

**X11** 的 XDamage/XShm/XTest 路径成熟，适合兼容和无人值守。**Wayland** 则遵循合成器安全边界：标准路径通过 XDG Portal 由用户/策略授权屏幕和输入，并从 PipeWire 获取流。[ScreenCast Portal](https://flatpak.github.io/xdg-desktop-portal/docs/doc-org.freedesktop.portal.ScreenCast.html)、[RemoteDesktop Portal](https://flatpak.github.io/xdg-desktop-portal/docs/doc-org.freedesktop.portal.RemoteDesktop.html)

风险来自 GNOME/KDE、Portal 后端、PipeWire/WirePlumber、显卡驱动、VA-API/NVENC、发行包格式和权限恢复差异。首版应只承诺两种 Ubuntu LTS 桌面组合，其他发行版标记社区支持/Beta。

### 5.4 Android：控制端高可行，被控端条件可行

控制 PC 的能力完整：硬件解码、触控映射、软键盘、蓝牙鼠标/键盘和系统文件选择器均可实现。

被控端可通过 MediaProjection 捕获屏幕、AccessibilityService 执行动作/手势，但 Android 14 及以上要求每次 MediaProjection 会话重新取得用户同意，锁屏或用户停止也会结束会话。[Android MediaProjection](https://developer.android.com/media/grow/media-projection)

**结论**：商店版 Android 被控定义为“用户在场并授权”；企业专有设备可进一步研究 Device Owner/OEM SDK，但不得把特权 OEM 能力写成通用功能。

### 5.5 iOS/iPadOS：控制端高可行，被控端只读可行

iPhone/iPad 作为控制端可实现硬件解码、触控板模式、外接键鼠和文件选择。作为被控端时，可在系统授权下共享画面，但通用第三方应用没有等同 PC 的系统级输入注入和常驻后台自由。

此外需要检查 App Store Remote Desktop Client 规则、账号与付费路径、隐私声明和具体 UI，参见[App Review Guidelines 4.2.7](https://developer.apple.com/app-store/review/guidelines/)。

**结论**：首版只宣传 iOS/iPadOS 控制 PC；被控屏幕共享作为后续受限功能，避免产品承诺与系统能力冲突。

## 6. 技术选型可行性与选择理由

### 6.1 自研、复用与分叉方案

| 方案 | 上手速度 | 性能上限 | 商业/许可 | 长期可控 | 结论 |
| --- | ---: | ---: | --- | ---: | --- |
| 直接 fork RustDesk | 高 | 中高 | 客户端 AGPL-3.0；闭源分发或 SaaS 修改需严肃评估义务/商业授权 | 中 | 仅适合接受 AGPL 或取得合适商业许可的路线，不作为默认建议 |
| 基于 VNC | 高 | 中低 | 实现多，许可各异 | 中 | 可做兼容协议，不适合作为 4K 高帧率核心 |
| 基于 RDP | 中 | 中高 | Windows 主机与授权边界明显 | 低 | 可做企业聚合入口，不能满足跨平台被控核心 |
| WebRTC + 自有产品协议 | 中高 | 高 | 标准和实现成熟，仍需审查具体库及编解码器许可 | 高 | 推荐首版 |
| 全自研 UDP/QUIC 媒体协议 | 低 | 最高 | 完全掌控，但安全和网络研发成本最大 | 最高 | 达到规模且 WebRTC 确认成为瓶颈后再做 |

选择 WebRTC 的核心原因不是“做视频通话”，而是一次获得 ICE/STUN/TURN、拥塞控制、加密媒体、可靠/非可靠数据通道、移动网络适配和成熟诊断数据。WebRTC 官方说明 PeerConnection 可承载视频、音频及任意二进制数据，ICE 通过 STUN/TURN 寻找路径。[WebRTC Peer Connections](https://webrtc.org/getting-started/peer-connections)

但必须用接口隔离：捕获与编码不能依赖 Flutter 插件内部实现，`Transport` 也不能泄露到业务层。后续如发现多屏 4K、文件大吞吐或端到端时延被 SCTP/现有拥塞算法限制，可在相同会话协议下增加 QUIC transport。

### 6.2 UI 方案

| 方案 | 优势 | 风险 | 判断 |
| --- | --- | --- | --- |
| Flutter | 五个平台统一业务 UI，响应式、触控与开发效率好；支持平台通道/FFI | 桌面多窗口、原生纹理和快捷键需专项开发 | 推荐 |
| Qt | 桌面与原生图形成熟，C++ 媒体集成直接 | 商业许可评估、移动 UI 效率和团队 C++ 成本 | 备选 |
| Electron/Tauri Web UI | Web 团队易上手，Chromium WebRTC 成熟 | Electron 不能覆盖 iOS/Android；需要第二套移动 UI，桌面包体/运行内存和高权限安全面更大 | 已排除，不作为核心客户端 |
| 各平台全原生 | 体验上限高 | 五套 UI 和业务状态，交付/一致性成本过高 | 仅在关键会话页面局部原生化 |

Flutter 官方支持 Android、iOS、Windows、macOS、Linux，并提供平台代码/FFI 集成路线。[Flutter 平台集成](https://docs.flutter.dev/platform-integration) 这证明 UI 复用可行，但不能证明媒体性能自然满足；因此视频帧必须通过原生 GPU 纹理渲染，Dart 只接收状态和统计。

最终选择 Flutter 的决定成立，理由是：

1. 原始范围包含 PC、手机和平板；Flutter 能保持一套页面、导航、状态、国际化和设计系统，Electron 只能解决桌面端。
2. 若选择 Electron，需要再引入 React Native/原生移动端，表面省下 Dart 学习成本，实际增加两套渲染体系、两套组件和更多端间差异。
3. 远控性能不依赖 Flutter 绘制视频。只要使用原生 Texture/Platform View，Dart 不接触 RGBA/YUV 帧，性能瓶颈仍是捕获、硬编、网络、硬解与显示同步。
4. Flutter 的主要新增成本是团队培训、原生插件、桌面多窗口与快捷键；这些是一次性、可通过模板和专项原型控制的成本，低于长期维护两套 UI。

必须设置一条退路：如果远程会话视图在两个原型周期后仍因 Flutter 原生纹理/多窗口集成无法达到 SLO，只把会话视图替换为独立原生窗口，登录、设备、文件和设置仍保留 Flutter，避免重写整个客户端。

### 6.3 客户端核心

Rust 适合网络协议、文件清单、权限状态和并发会话；平台 API 仍使用 C++/Swift/Kotlin/C 适配。选择混合方案而非“全部 Rust”的原因是硬件媒体和系统 SDK 原生接口最成熟，强行纯语言化会制造不必要的封装成本。

主要风险是 WebRTC C++、Rust core 和 Flutter 三层 FFI 的生命周期与线程模型。阶段 0 必须验证：纹理句柄跨层、回调线程、销毁顺序、热重连、编码器重建和崩溃隔离。如果该边界在 5 周内仍不稳定，应退回“C++ media engine + C ABI + Rust 只做业务协议”的更窄边界。

### 6.4 Java 服务端

Java/Spring Boot 模块化单体 + PostgreSQL + Redis + coturn 对 MVP 足够，并比引入 Go 更符合当前团队结构。该选择的关键依据是：

- 控制平面只处理认证、设备、授权、信令、策略和审计，不采集、转码或正常转发屏幕媒体，因此 Java/JVM 不决定帧率和端到端画面延迟。
- 现有 Java 人员可以直接复用 Spring Security、OIDC/OAuth2、事务、数据库迁移、测试和运维经验，减少招聘、培训和双语言值班成本。
- Spring Boot 适合模块化单体；WebSocket 信令可使用 Spring 的异步 WebSocket/WebFlux + Reactor Netty 路径，并用 Redis 处理在线状态、短期票据、限流和跨实例路由。Spring 官方文档确认 WebFlux 支持非阻塞处理、Reactive Streams 背压和 Netty，并提供响应式 WebSocket API。[Spring WebFlux](https://docs.spring.io/spring/reference/web/webflux.html)、[WebFlux WebSocket](https://docs.spring.io/spring-framework/reference/web/webflux-websocket.html)
- TURN 是主要带宽热点，继续使用独立 coturn 扩缩容；不使用 Java 重写成熟的 STUN/TURN UDP 数据面。
- 过早微服务化会引入分布式事务、部署和可观测性成本，却不会降低媒体延迟。

Java 方案的风险不是业务吞吐，而是错误地把阻塞数据库/对象存储操作放进 WebSocket 事件循环，以及无界发送队列造成慢客户端拖垮进程。必须限制消息大小、队列、连接与租户配额，设置心跳/半开连接回收，对阻塞调用做线程隔离，并用 Micrometer/OpenTelemetry 观测 JVM、连接数、事件循环、数据库和 Redis。

MVP 使用同一代码库和部署单元。达到单区域 5 万长连接，或压测证明信令与 REST/审计资源模型互相干扰时，再从同一 Java 项目拆出 `signaling-service`；不能仅因为“未来可能扩容”提前拆微服务。

### 6.5 最终技术栈与语言成本

| 层 | 最终选择 | 主要语言 | 人员边界 |
| --- | --- | --- | --- |
| 跨端 UI | Flutter | Dart | Flutter 工程师负责页面、状态和响应式，不处理媒体帧 |
| 客户端核心 | Rust crates | Rust | 会话、协议、安全、文件和传输抽象 |
| 桌面媒体适配 | 原生 API/WebRTC bridge | C/C++ | Windows/Linux 采集、输入、硬件媒体；macOS 通过 Swift 桥接 |
| Apple 平台 | Flutter plugin + 原生适配 | Swift | ScreenCaptureKit、VideoToolbox、权限和系统集成 |
| Android | Flutter plugin + 原生适配 | Kotlin | MediaProjection、MediaCodec、AccessibilityService |
| 控制平面 | Spring Boot 模块化单体 | Java | 复用现有后端团队；负责控制面，不处理媒体热路径 |

主要语言仍有六种，但没有六套重复业务：Dart、Rust、Java 是主要业务语言，C/C++、Swift、Kotlin 是薄平台适配。选择 Java 消除了额外的 Go 人员成本；选择 Flutter 则用一次性培训成本换取五个平台长期共用 UI。

## 7. 性能可行性

### 7.1 原始数据量证明必须使用 GPU 路径

不含压缩时：

- 1080p60 BGRA：`1920 × 1080 × 4 × 60 ≈ 497.7 MB/s ≈ 3.98 Gbit/s`
- 2K60 BGRA：`2560 × 1440 × 4 × 60 ≈ 884.7 MB/s ≈ 7.08 Gbit/s`
- 4K60 BGRA：`3840 × 2160 × 4 × 60 ≈ 1.99 GB/s ≈ 15.93 Gbit/s`

只要在采集、转换、编码之间多做一次 CPU 全帧拷贝，4K60 就可能吞噬内存带宽并显著增加延迟。因此“GPU surface 到硬件编码器”和“硬解纹理到显示”的零/少拷贝不是优化项，而是 4K 可行性的前提。

### 7.2 网络预算

实际码率高度依赖内容、编码器、质量和色彩，以下只作为容量设计起点，必须用原型修正：

| 模式 | H.264 设计区间 | HEVC 设计区间 | 备注 |
| --- | ---: | ---: | --- |
| 1080p60 办公/混合 | 6–20 Mbps | 4–14 Mbps | 静态文本低、滚动和视频高 |
| 2K60 | 12–35 Mbps | 8–25 Mbps | 设计/游戏需要更高上限 |
| 4K30 | 15–40 Mbps | 10–30 Mbps | 双端必须硬编硬解 |
| 4K60 | 25–70 Mbps | 18–50 Mbps | 4:4:4/HDR 会进一步提高 |

Parsec 官方对 1080p60 建议主机和客户端具备约 30Mbps 网络条件，并建议低 RTT，这可作为保守参照而非 CrossDesktopRemote 的最终值。[Parsec 网络要求](https://support.parsec.app/hc/en-us/articles/32381460716180-Parsec-Connectivity-Requirements)

### 7.3 延迟预算

1080p60 的理想硬件路径预算：

| 阶段 | 目标预算 |
| --- | ---: |
| 等待下一次采集/桌面变化 | 0–16.7ms |
| GPU 转换 + 编码 | 3–10ms |
| 网络单向 | 约 RTT/2 + 抖动缓冲 |
| 解码 | 2–8ms |
| 渲染等待 | 0–16.7ms |

因此局域网输入到画面 P95 目标 45–60ms 是有挑战但合理的；“公网端到端固定 3ms/7ms”在物理网络传播和 60Hz 帧周期下不能作为通用承诺。应同时记录管线延迟与网络 RTT，避免口径混淆。

### 7.4 4K 实现门禁

只有同时满足以下条件才在 UI 开放 4K：

1. 被控端捕获 surface 与编码器可走 GPU，编码器报告支持目标分辨率/FPS。
2. 控制端硬解和渲染能力通过运行时探测，移动设备温控测试合格。
3. 显示器实际需要 4K，窗口过小时自动请求较低编码分辨率。
4. 上行带宽估计高于当前目标码率 1.5 倍，丢包和 RTT 在阈值内。
5. 运行 10 秒预热没有编码排队、持续掉帧或热降频。
6. 若不满足，自动降到 2K/1080p，并明确显示原因。

### 7.5 原型实验

| 实验 | 方法 | 通过标准 | 失败后的动作 |
| --- | --- | --- | --- |
| GPU 零拷贝 | Windows DXGI→硬编，记录每阶段与显存/CPU | 1080p60 CPU 均值合理且无帧队列增长；4K30 稳定 30 分钟 | 调整 surface 格式/共享句柄；必要时缩窄媒体核心语言边界 |
| 端到端延迟 | 高速摄像机/LED+输入装置与软件时间戳双测 | LAN P95 ≤ 60ms，统计误差可解释 | 优先修帧排队、VSync 和编码配置，不扩平台 |
| NAT 成功率 | 家宽、企业网、CGNAT、对称 NAT、UDP 封锁 | 含 TURN 总连接成功率 ≥ 98% | 增加 TCP/TLS 443 中继与区域调度 |
| 弱网 | 30/80/150ms RTT，1/3/5% 丢包，突发抖动 | 3% 丢包下无持续 1s 卡死，输入不断连 | 调整码控、关键帧、NACK/FEC 与 QoS |
| 移动端解码 | 中低高端 Android/iPhone/iPad 30 分钟 | 1080p60 无持续热降频；触控误差可接受 | 设备能力分级、降分辨率/FPS |
| 文件恢复 | 20GB 文件传输中断、磁盘满、重复/恶意路径 | 恢复后 SHA-256 一致，无越界写入 | 修协议，不允许带缺陷进入公开测试 |

## 8. 网络与基础设施可行性

### 8.1 连接策略

推荐顺序：同 LAN 直连 → IPv6/IPv4 P2P UDP → 区域 TURN/UDP → TURN/TCP/TLS 443。ICE 是 STUN/TURN NAT 穿透的标准机制，[RFC 8445](https://datatracker.ietf.org/doc/html/rfc8445)说明了其候选与检查流程。

连接率目标应拆开：

- 总连接成功率：≥ 98%，成熟期 ≥ 99.5%。
- P2P 成功率：先测基线，不承诺固定百分比；按运营商、国家、路由器和网络类型分桶。
- 首帧 P95、重连成功率和路线回退比例需要作为核心运营指标。

### 8.2 中继带宽经济性

中继是主要可变成本。单路 20Mbps 会话一小时的数据量约为：

`20 Mbit/s ÷ 8 × 3600 ≈ 9 GB/会话小时`

若 100 路会话全部中继、每天 8 小时，约为 `7.2 TB/天` 的转发流量。4K60 若平均 40Mbps，数值翻倍。正式预算按以下公式计算：

`月中继流量 = 中继并发 × 平均码率 × 每日小时 × 30 ÷ 8`

因此必须：

- 提高 P2P 命中，但不能牺牲安全去默认开放危险的 UPnP 策略；
- 按延迟和负载选择区域中继；
- 为套餐设置分辨率/峰值码率/并发与公平使用策略；
- 将文件传输设为低优先级，并允许企业把中继部署到自有网络；
- 对超大文件可在用户明确同意后选择可恢复的加密对象存储中转，但这不是默认“原文件直传”路径。

### 8.3 容量与高可用

- 控制平面至少 2 实例、PostgreSQL 主备、Redis 高可用；在线状态允许最终一致。
- TURN 无状态化部署，凭短期凭据授权；健康检查剔除节点，新会话迁移到健康节点。
- 已建立会话不依赖每秒查询数据库；控制平面短暂不可用时不切断会话。
- 每区域预留 30% 带宽余量，使用真实 95 分位码率而非配置上限估算。
- 私有部署先提供单节点 Docker Compose/Helm 参考，再提供高可用拓扑；不要让两套产品代码分叉。

## 9. 功能可行性

### 9.1 原文件传输

可靠 DataChannel 支持二进制数据，分块、哈希、恢复位图和临时文件即可实现原文件一致性。主要风险不是“能不能传”，而是：

- 大文件和视频争抢拥塞窗口；
- 断点状态损坏或重复写；
- Unicode、超长路径、符号链接和平台权限差异；
- 移动沙箱和磁盘空间；
- 恶意文件、路径穿越和审计隐私。

使用独立低优先级通道、最终 SHA-256、原子落盘和路径规范化后，风险可控。该功能适合 MVP。

### 9.2 多显示器

PC API 都可枚举显示器，难点是 DPI/旋转/负坐标/热插拔、同时编码带宽和移动端多路解码。采用“每屏独立 track + 动态订阅”可行；默认不同时发送所有屏的全分辨率流。

### 9.3 剪贴板

文本双向同步可快速交付；图片、富文本和文件剪贴板逐步增加。必须有方向策略、大小限制、回环抑制和会话清理。企业环境默认允许策略禁用。

### 9.4 设备信息与截图

系统信息采集技术简单，隐私边界更重要。只上报能力和健康所需最小字段。截图视为新的敏感操作权限，避免设备列表后台持续上传用户桌面。

### 9.5 会话记录

连接元数据日志容易实现，应默认开启。画面录像可直接封装已编码流，计算成本低，但会引入磁盘、密钥、保留策略、通知和法律合规问题，因此默认关闭并显式提示。

## 10. 安全可行性

### 10.1 能达到的安全基线

使用 WebRTC 并不自动等于产品整体安全。可行的首版基线是：

- 设备独立身份签名密钥（Ed25519 默认，硬件密钥存储可用 P-256）；签名绑定双方、权限、有效期和 DTLS fingerprint。
- WebRTC DTLS-SRTP/DataChannel 加密；TLS 保护 API/WSS；TURN 不解密内容。
- 单次短期会话票据、nonce、重放保护、连接限速。
- 账号 MFA、新设备确认、无人值守 allowlist 和细粒度 scope。
- 高权限 Agent/驱动最小化，本机 IPC 认证。
- 安装/更新签名、SBOM、依赖扫描、协议与文件解析 fuzz。
- 会话可见指示、紧急断开、截图/录制/文件独立授权。

Parsec 官方描述其 P2P 音视频/输入使用 DTLS 1.2 加密并通过一次性连接 token 验证，这证明相似链路设计可行。[Parsec Security](https://support.parsec.app/hc/en-us/articles/32361366289940-Security-At-Parsec)

### 10.2 不应采用的捷径

- 只用 6 位临时码直接派生媒体密钥。
- 将无人值守永久密码可逆加密存储在云端或主机配置文件。
- 中继解密全部会话，仍宣传端到端加密。
- 为了“无人值守”隐藏系统捕获提示、托盘状态或绕过平台授权。
- 让 SYSTEM/root 进程解析视频、图片、压缩包和远端文件名。
- 日志记录键盘内容、剪贴板正文、文件块或会话密钥。

### 10.3 安全资源

至少需要 0.5–1 名专职或长期顾问安全工程师，在设计、MVP 前和公开测试前三次介入。公开测试前应完成威胁建模、代码审计、依赖审计和第三方渗透测试；高权限驱动和自动更新单独评审。

## 11. 许可证、商店与合规可行性

### 11.1 开源许可证

- RustDesk 仓库标记为 AGPL-3.0。[RustDesk GitHub](https://github.com/rustdesk/rustdesk) 若 CrossDesktopRemote 拟闭源商业化，不应直接复制其代码、协议实现或修改后分发而忽略许可证；可以研究公开架构思想，代码复用需法律审查或商业授权。
- Flutter、具体 WebRTC 构建、coturn、FFmpeg/GStreamer、编解码包装库等均需形成 SBOM，并逐项确认许可证和编译选项。
- 使用 FFmpeg 时，是否启用 GPL 组件会影响整体分发条件；不能只看 FFmpeg 项目名称就下结论。

### 11.2 编解码器与驱动

H.264/HEVC 可能涉及专利池、系统许可、分发地区和商业模式。优先调用操作系统/硬件厂商提供的编码 API 能降低工程风险，但不自动消除产品层面的许可义务。正式商业发布前需要知识产权律师确认：编码、解码、云服务、客户端分发和付费模式分别是否需要许可。

虚拟显示器、虚拟输入和系统服务需要 Windows 代码签名/可能的驱动签名，macOS 需要 Developer ID、公证和相应 entitlement；这些是发布日期前置路径，不是打包阶段的一天工作。

### 11.3 隐私与会话录像

需要明确数据角色、收集目的、保留期、删除和导出机制。媒体内容默认 P2P 不落云；设备信息最小化；录像与截图有持续指示和策略；跨境/行业合规按首发地区另做 DPIA/法律评估。

### 11.4 应用商店

- iOS/iPadOS 的 Remote Desktop Client、账号创建、支付方式、隐私清单和后台能力需要提前做审核样品，而不是完工后再提交。
- Android 被控使用 AccessibilityService 时必须有清楚的用户价值、披露和符合商店政策的使用方式；不能用于隐蔽监控。
- 桌面端也需要签名、自动更新回滚与恶意软件误报处理流程。

## 12. 团队、周期与成本

### 12.1 建议团队

| 角色 | 人数 | 主要职责 |
| --- | ---: | --- |
| 技术负责人/架构 | 1 | 媒体、协议、平台边界与质量门禁 |
| 系统/媒体工程师 | 3 | Windows/macOS/Linux 捕获、编码、输入、渲染 |
| Rust/实时网络工程师 | 1–2 | session core、WebRTC 集成、文件/剪贴板协议 |
| Flutter/移动工程师 | 2 | 跨端 UI、原生纹理、触控、移动平台适配 |
| Java 后端/SRE | 2 | Spring Boot 控制平面、WebSocket 信令、TURN 调度、可观测性、部署 |
| QA/自动化 | 2 | 设备矩阵、网络实验室、性能和回归 |
| 安全工程师 | 0.5–1 | 威胁模型、身份、Agent、更新和审计 |
| 产品/UX | 1 | 场景、权限提示、响应式和可用性 |

完整配置约 12–14 人；若部分负责人兼任，可压缩为 10–11 人，但系统媒体、移动、后端、QA 和安全不能全部由一两个人长期兼任。

当前项目实际为单人开发，不能套用多人并行排期。近期采用“一个设备组合、一个显示器、一个局域网、一个有人值守流程”的纵向切片：先完成 iPad 控制 Mac，再进入 Windows 性能原型。单人阶段应优先复用维护活跃的 WebRTC 插件和系统 API，并通过时间盒实验决定哪些路径值得替换为自研原生实现。

### 12.2 现实时间线

| 时间 | 可交付状态 |
| --- | --- |
| 第 1–5 周 | iPad→Mac LAN 有人值守原型：画面、触控、权限和性能数据 |
| 第 6–11 周 | Windows LAN/P2P 性能原型与阶段门禁数据 |
| 第 3–6 月 | Windows 功能 MVP：远控、文件、剪贴板、授权、基础服务端 |
| 第 7–10 月 | Windows 稳定版、Android 控制端 Beta、macOS Beta、安全整改 |
| 第 8–11 月 | macOS 稳定、iOS/iPadOS/Android 控制端稳定、2K/4K 能力灰度 |
| 第 10–14 月 | Linux 受支持矩阵、Android 受限被控、企业策略与私有部署 Beta |
| 第 12–18 月 | 多区域、高可用、企业审计、兼容长尾、外部安全和生产成熟度 |

阶段可以并行，但不能跳过门禁。实现方案中的周数是各阶段在相应人员并行投入下的工程历时，不代表单人串行工期。

### 12.3 成本模型

由于团队所在地、云区域和目标并发未知，不给出虚假精确金额。预算至少包含：

- 人力：系统媒体与跨平台 QA 是最大固定成本。
- 云：TURN 出口带宽是最大可变成本；其次是日志、对象存储和多区域数据库。
- 设备实验室：不同 GPU/驱动的 Windows PC、Intel/Apple Silicon Mac、中低高端 Android、不同 iPhone/iPad、路由器和多显示器。
- 证书与发布：Apple/Windows 签名、可能的扩展验证/驱动签名。
- 法律与安全：开源/编解码许可、隐私、商店政策、渗透测试。
- 支持：远控软件天然涉及网络、权限、驱动和误报，大规模发布后支持成本不可忽略。

## 13. 逐步选择理由与执行计划

| 步骤 | 选择 | 原因 | 计划/产物 | 通过门禁 |
| --- | --- | --- | --- | --- |
| 0. 冻结产品边界 | PC 完整被控，全平台控制；移动被控条件化 | 避免不可实现的全对称承诺 | 支持矩阵、术语、首发场景和非目标 | 产品与研发共同签字，营销文案不超范围 |
| 1. 建性能测量台 | 先测再扩功能 | 低延迟是核心差异，无法后补 | 硬件清单、场景视频、时间戳与高速摄像机方法 | 数据可重复，误差有说明 |
| 2. Apple 纵向原型 | iPad 控制 Mac；项目内 `flutter_webrtc` fork + ScreenCaptureKit + Core Image/Metal/vImage + 原生 Texture | 适合单人和现有设备；fork 可明确 SDR/Rec.709/Range，并以 Active/Pending/Retired 短期双采集保证跨显示器切换可回滚，同时保持单 RTC Track/Sender | PostEvent 权限、触控/IME、系统/快捷双键盘、采集到渲染的目标几何事务、目标流双帧预热、控制端确认提交/超时回滚、采集事务 epoch 与画质/切屏串行化、SCK `contentRect × scaleFactor` 裁剪及居中规范化、自动画质 Sender 快调与 SCK 延迟合并、单一呈现/触控几何、稳定画布与原子提交、三阶段色彩/SCK 几何/区间媒体诊断、条件 HDR→SDR、8 秒断线恢复和 30 分钟报告 | 基本连接/画面/输入已由用户验证；73 个 Flutter 测试、静态分析及 macOS/iOS Debug 构建通过；主屏/Sidecar 与自动档交叉切换、双采集峰值开销、提交/回滚资源释放、有效区域居中与直接触控对齐、自动升降档、SDR/HDR、Sidecar 清晰度和 60 FPS P95 仍需物理复验 |
| 3. WebRTC/Java 信令原型 | ICE/STUN/TURN + Java WebSocket 信令 + 分通道 | 同时验证连接率、弱网、安全传输与既有后端技术栈 | 5 分钟单次连接码、邀请/来源两级限流、P2P/Relay、网络切换和连接清理报告 | TTL/消费/两级限流与新码解封的 9 个 Java 测试已通过；生产前补身份、Redis 限流、短期票据和 WSS |
| 4. Windows 零拷贝原型 | DXGI + 硬编硬解 | Apple 纵向闭环后再验证 Windows 核心性能路径 | 1080p60/2K60/4K30 demo、阶段延迟 | 达到 MVP SLO，否则暂停扩平台 |
| 5. 定义协议与权限 | Protobuf + capability + scope | 保证跨版本、跨平台和最小权限 | `proto/`、状态机、错误码、上限 | 兼容性测试与安全评审通过 |
| 6. Windows MVP | 先完成一个纵向切片 | 多平台半成品无法验证真实用户价值 | Flutter UI、Java 控制平面、安装、设备、远控、文件、剪贴板、记录 | 50 台内测、会话无崩溃率和连接率达标 |
| 7. 安全加固 | 设备密钥、MFA、签名、Agent 隔离 | 无人值守扩大攻击面 | 威胁模型、fuzz、更新与第三方测试 | 阻断/高危问题清零或有明确上线阻断 |
| 8. 移动控制端 | 先做 iPad/Android tablet，再手机 | 平板最接近桌面生产力，手机需更多交互折衷 | 三套响应式布局、触控/键盘/文件 | 真实设备 30 分钟稳定与可用性测试 |
| 9. macOS | ScreenCaptureKit/VideoToolbox | 性能路径明确，扩展高价值用户 | 权限引导、多屏、键鼠、签名/公证 | 新装/升级/撤权恢复场景通过 |
| 10. Linux | Ubuntu GNOME/KDE 收敛矩阵 | 避免发行版组合爆炸 | Wayland Portal、X11 fallback、硬编矩阵 | 只对已通过组合标 Stable |
| 11. 4K 与专业模式 | capability 白名单开放 | 防止低端设备被营销目标拖垮 | 4K30/60、HEVC、质量模式 | 30 分钟温控/稳定、降级正确 |
| 12. 企业与私有化 | 稳定核心之上增加治理 | 企业价值依赖策略审计，但不应阻塞核心原型 | RBAC/SSO/策略/审计/部署包 | 租户隔离、安全与灾备演练通过 |
| 13. 自研协议评估 | 只在数据证明 WebRTC 瓶颈时启动 | 避免重复制造成熟轮子 | WebRTC 限制报告、QUIC 原型、收益测量 | 至少在目标场景有显著、可复现收益 |

## 14. 主要风险与缓解

| 风险 | 概率 | 影响 | 缓解与触发动作 |
| --- | --- | --- | --- |
| 4K60 在部分设备上编码/解码失败或过热 | 高 | 高 | 能力探测、白名单、自动降级；不把所有设备 4K 写入承诺 |
| Flutter/FFI/原生纹理生命周期不稳定 | 中 | 高 | 阶段 0 验证；缩窄 C ABI；媒体不进 Dart 堆 |
| macOS HDR 桌面映射到 SDR 时泛白或裁切 | 中 | 高 | 原始帧与编码/解码检查点分离；Mac Core Image/Metal HDR→SDR 与 `420v` 归一化；iPad Core Image/vImage BT.709 转换；HDR A/B、灰阶和 GPU 开销物理验收 |
| 团队缺少 Flutter 经验导致首期效率下降 | 中高 | 中 | 先建组件/插件模板；安排培训与 code owner；首版减少复杂动画和自定义渲染 |
| Java WebSocket 中执行阻塞调用或队列无界 | 中 | 高 | 事件循环与阻塞池隔离；队列/消息/租户配额；慢客户端断开；压测和 JVM 指标告警 |
| 对称 NAT/企业防火墙导致 P2P 失败 | 高 | 中 | 多区域 TURN/UDP + TCP/TLS 443；连接诊断 |
| 中继流量成本失控 | 中高 | 高 | P2P 优先、码率策略、套餐上限、企业自建 relay |
| Windows UAC/登录桌面、macOS 权限、Wayland 授权长尾 | 高 | 高 | 平台专用 Agent/适配层；收敛支持矩阵；权限健康检查 |
| Android/iOS 被控能力被误解 | 高 | 高 | 产品矩阵和 UI 明示；不做越权承诺 |
| 高权限 Agent 漏洞 | 中 | 极高 | 最小化进程、IPC 鉴权、不解析复杂远端数据、审计/fuzz/渗透 |
| 临时码诈骗与远控滥用 | 高 | 极高 | 身份显示、风险提示、限速、可见指示、举报和快速封禁机制 |
| RustDesk/FFmpeg/编码专利许可问题 | 中 | 高 | clean-room、SBOM、法律审查、商业许可预算 |
| Linux 发行版和 GPU 组合过多 | 高 | 中高 | 只承诺 LTS + 指定桌面/驱动，其他 Beta |
| 日志/截图/录像造成隐私风险 | 中 | 高 | 最小化、默认不录制、客户端加密、TTL、访问审计 |
| 团队低估 QA 与支持 | 高 | 高 | 从阶段 0 配 QA；设备实验室；灰度与诊断包 |

## 15. 项目阶段门禁

### 第一次：性能原型后

满足以下条件才进入完整 MVP：

- Windows 1080p60 在指定中端硬件达到局域网 P95 ≤ 60ms。
- 连续 30 分钟无帧队列增长、显著内存泄漏或编码器崩溃。
- 4K30 至少在两类 GPU 的硬件白名单上可运行，或明确决定 V1 延后 4K。
- P2P、TURN/UDP、TURN/TLS 都能建立和恢复会话。
- Flutter/原生纹理/Rust core 边界可稳定销毁和重连。

### 第二次：Windows MVP 内测后

- 总连接成功率 ≥ 98%，首帧 P95 ≤ 4s。
- 会话无崩溃率 ≥ 99.5%，没有阻断级文件损坏。
- 权限模型、设备撤销、临时码限速和更新签名通过安全评审。
- 50 台、至少 10 种网络和 6 种 GPU/驱动组合完成内测。
- 用户能自行完成权限、连接、文件传输和故障诊断，不依赖研发手把手处理。

### 第三次：公开发布前

- 会话无崩溃率 ≥ 99.9%，总连接成功率目标 ≥ 99.5%。
- 高危安全问题清零，完成第三方渗透测试与修复复测。
- 隐私政策、开源清单、编解码/商店/签名法律审查完成。
- 中继容量、告警、灾备、封禁滥用、密钥轮换和更新撤回均演练。
- 客服拥有可脱敏导出的诊断包和明确支持矩阵。

若第一次门禁无法达到，不应继续用“多加服务器”掩盖客户端媒体管线问题；若第二次门禁失败，不应同时扩 Linux 和移动被控端；若许可证或更新安全未完成，不应公开商业发布。

## 16. 最终建议

建议立项，但采用“性能纵向切片优先”的路线：先用 3–5 周完成 Windows 端到端原型并产出实测报告，再决定全平台投入。最值得守住的三条原则是：

1. **平台能力诚实**：跨平台不等于完全对称，iOS/iPadOS 与 Android/Wayland 的限制必须成为产品定义的一部分。
2. **性能可观测**：所有低延迟、帧率和画质都通过分阶段指标、硬件白名单和弱网实验验证。
3. **安全默认开启**：远控 Agent 拥有高敏感权限，设备身份、最小授权、可见提示、更新签名和审计必须与首个功能一起实现。

在此前提下，CrossDesktopRemote 的技术路线、市场场景和商业扩展方向成立；最大不确定性不是某个 API 是否存在，而是团队能否持续处理跨平台长尾、基础设施流量成本与高权限软件的安全责任。

## 17. 主要资料来源

- [向日葵产品功能](https://sunlogin.oray.com/product/feat?ici=SL_HOME_BANNER)
- [向日葵企业安全](https://sunlogin.oray.com/product/security)
- [ToDesk 功能总览](https://www.todesk.com/special.html)
- [ToDesk 文件传输](https://www.todesk.com/special_highspeedfiletransmission.html)
- [Parsec Overview](https://support.parsec.app/hc/en-us/articles/32361354307348-Overview)
- [Parsec Feature Matrix](https://support.parsec.app/hc/en-us/articles/32381463419924-Feature-Matrix)
- [Parsec Connectivity Requirements](https://support.parsec.app/hc/en-us/articles/32381460716180-Parsec-Connectivity-Requirements)
- [Parsec Security](https://support.parsec.app/hc/en-us/articles/32361366289940-Security-At-Parsec)
- [TeamViewer Product Description](https://www.teamviewer.com/en-us/legal/product-descriptions/teamviewer-remote-access-support/)
- [TeamViewer Security](https://www.teamviewer.com/en/special/security-explained/)
- [RustDesk Documentation](https://rustdesk.com/docs/en/)
- [RustDesk GitHub / AGPL-3.0](https://github.com/rustdesk/rustdesk)
- [AnyDesk Performance](https://anydesk.com/en/performance)
- [Microsoft Desktop Duplication API](https://learn.microsoft.com/en-us/windows/win32/direct3ddxgi/desktop-dup-api)
- [Microsoft SendInput](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-sendinput)
- [Apple ScreenCaptureKit](https://developer.apple.com/documentation/ScreenCaptureKit)
- [Apple App Review Guidelines](https://developer.apple.com/app-store/review/guidelines/)
- [Android MediaProjection](https://developer.android.com/media/grow/media-projection)
- [XDG Desktop Portal ScreenCast](https://flatpak.github.io/xdg-desktop-portal/docs/doc-org.freedesktop.portal.ScreenCast.html)
- [XDG Desktop Portal RemoteDesktop](https://flatpak.github.io/xdg-desktop-portal/docs/doc-org.freedesktop.portal.RemoteDesktop.html)
- [WebRTC Peer Connections](https://webrtc.org/getting-started/peer-connections)
- [W3C WebRTC Recommendation](https://www.w3.org/TR/webrtc/)
- [RFC 8445 ICE](https://datatracker.ietf.org/doc/html/rfc8445)
- [Flutter 平台集成](https://docs.flutter.dev/platform-integration)
- [Spring WebFlux](https://docs.spring.io/spring/reference/web/webflux.html)
- [Spring WebFlux WebSocket](https://docs.spring.io/spring-framework/reference/web/webflux-websocket.html)
- [Spring Boot Metrics / Micrometer](https://docs.spring.io/spring-boot/reference/actuator/metrics.html)
