# CrossDesktopRemote 工程搭建

> 更新日期：2026-08-17

## 1. 当前结果

| 模块 | 状态 | 生成方式 |
| --- | --- | --- |
| 顶层 monorepo 目录 | 已建立 | 目录与说明文件 |
| Java 控制平面 | Apple 原型信令通过 | Flyway、健康检查、双端 WebSocket 中继和集成测试通过 |
| Flutter 客户端 | Apple 原型构建通过 | WebRTC 会话、macOS/iOS Simulator/物理 iPad build 通过 |
| Rust workspace | 本机基础验收通过 | 六个 crate、C ABI、macOS/Android 构建和测试通过 |
| Protobuf | v1 基线已建立 | Buf lint 与 Java/Rust/Dart 生成、编译检查通过 |
| 平台原生层 | 目录已建立 | 后续使用平台官方工具创建工程 |
| 本地基础设施 | 本机基础验收通过 | PostgreSQL、Redis、coturn Compose 健康 |
| 统一验收 | 通过 | `scripts/check-all.sh` 全流程成功 |

## 2. 已检测工具链

当前机器已发现：

- Flutter 3.47.0 stable、Dart 3.13.0：`/Volumes/zhiti-1T/Library/flutter/bin`
- Rust/Cargo 1.97.1：`/Users/chenlihao/.cargo/bin`
- Protobuf Compiler 35.1、Buf 1.72.0、`protoc-gen-dart` 25.0.0
- OpenJDK 17、Gradle 8.13
- Docker 28.0.1、Docker Compose 2.33.1
- CMake 4.2.0
- Android SDK 36.0.0、Platform 35/36、NDK 28.2.13676358、CMake 3.22.1/4.2.0、`cargo-ndk` 4.1.2
- Xcode 26.6、CocoaPods 1.17.0

用户 Shell 已配置 Flutter/Rust PATH；若某个非交互进程未继承环境，可显式使用 `CROSSDESKTOP_FLUTTER_BIN` 和工具绝对路径。统一检查会在 `PATH`、环境变量及已知安装目录中解析工具。

Android 和 Apple 工具链已通过 `flutter doctor`。Chrome 缺失只影响当前未纳入目标的平台 Web。Docker Desktop 已启动，本地三个依赖服务均健康。

可重新检查：

```bash
./scripts/check-toolchains.sh
```

## 3. Spring Boot 控制平面

项目使用 Spring Boot 4.1.0、Java 17、Gradle Groovy DSL。工程由 `start.spring.io` 生成，包含 Web MVC、WebSocket、Validation、Security、Actuator、JPA、Redis、PostgreSQL 和 Flyway。

复现命令：

```bash
./scripts/bootstrap-spring.sh
```

生成器提供 Gradle 9.5.1 Wrapper，因此应使用工程内 Wrapper，不依赖机器上的 Gradle 8.13：

```bash
./services/control-plane-java/gradlew \
  -p services/control-plane-java \
  tasks
```

已增加：

- `application-local.yaml` 和 `application-test.yaml`，连接本地 Compose 的 PostgreSQL/Redis。
- `V1__control_plane_baseline.sql`，建立 Flyway 基线表。
- 最小安全策略，只匿名放行 `/actuator/health`，其他请求默认拒绝。

已验证 `gradlew test` 成功；`SPRING_PROFILES_ACTIVE=local ... bootRun` 能连接 PostgreSQL/Redis、执行 Flyway V1，`/actuator/health` 返回 `UP`，根路径返回 `403`。这证明控制平面基座和开发态信令可运行，不代表身份、设备注册或生产会话授权已经实现。

M1 已增加 `/ws/signaling` 开发态端点：六位房间码、host/controller 双角色、消息类型白名单和 64KiB 上限。集成测试使用两个真实 WebSocket 客户端验证双向中继和不同加入顺序。该端点没有身份、TTL、WSS 或 Redis 跨实例路由，只能用于局域网原型。

用户级 `~/.gradle/init.gradle` 当前通过 Settings 级仓库、`PREFER_SETTINGS`、阿里云 Maven 和 Flutter 国内镜像解决本机网络问题。该文件会影响本机所有 Gradle 工程，只是开发机网络适配，不是仓库运行所依赖的隐式配置。

## 4. Flutter 客户端

Flutter 五平台工程已经由 stable SDK 官方 CLI 生成。安装与生成参考：

- https://docs.flutter.dev/get-started/install
- https://docs.flutter.dev/reference/create-new-app

新环境复现：

```bash
flutter doctor -v
./scripts/bootstrap-flutter.sh
```

脚本等价于在 `apps/client_flutter` 中调用：

```bash
flutter create \
  --empty \
  --org com.crossdesktopremote \
  --project-name cross_desktop_remote \
  --platforms android,ios,linux,macos,windows \
  --android-language kotlin \
  --ios-language swift \
  .
```

Flutter UI 不承担逐帧视频拷贝；后续媒体画面必须接入原生 Texture。

2026-08-17 验证结果：

- Flutter 3.47.0、Dart 3.13.0。
- `flutter pub get` 成功。
- `flutter analyze` 无问题。
- `flutter test` 成功：14 个常规测试通过；设置 Rust 动态库路径后另有 1 个真实 FFI 测试。
- `flutter build macos --debug` 成功。
- `flutter build apk --debug` 成功，产物为 `build/app/outputs/flutter-apk/app-debug.apk`。
- `flutter build ios --simulator --debug` 成功；接入 SwiftPM 插件后的当前产物为 `build/ios/Debug-iphonesimulator/Runner.app`。
- `flutter build ios --debug --no-codesign` 成功，验证物理设备 arm64 代码。
- 物理 iPad 自动签名、安装和 `devicectl` 启动成功。
- `flutter doctor` 认可 Android SDK 36.0.0、Xcode 26.6 和 CocoaPods 1.17.0。
- Android 构建已安装 NDK `28.2.13676358`、Android SDK Platform 35/36 和 CMake `3.22.1`；项目级 `settings.gradle.kts` 在不改动用户级镜像的前提下，仅为 `flutter_webrtc` 的 `com.github.davidliu` 传递依赖补充 JitPack 仓库。
- Android Debug APK 已包含 `arm64-v8a`、`armeabi-v7a`、`x86_64` 三个 `libcrossdesktop_core.so`；尚未在 Android 真机上验证运行时 FFI。
- macOS FFI 测试通过显式动态库路径加载；应用发布包内自动嵌入/签名仍待实现。
- `flutter_webrtc 1.6.0` 已接入并完成 macOS/iOS 构建；Mac 屏幕录制、辅助功能和 iPad Local Network 权限必须在物理设备上人工授权。

## 5. Rust workspace

Rust/Cargo 已安装并完成 crate 生成。新环境安装参考：

- https://www.rust-lang.org/tools/install
- https://rustup.rs

新环境复现：

```bash
rustup default stable
rustc --version
cargo --version
./scripts/bootstrap-rust.sh
```

脚本对每个现有目录使用 `cargo init --lib --edition 2024 --vcs none`，不会创建嵌套 Git 仓库；完成后建立根 Cargo workspace。当前 workspace 包含 `session-core`、`protocol`、`media-core`、`transfer-core`、`security-core` 和 `client-ffi`。

2026-08-17 已验证：

- Rust/Cargo 1.97.1。
- `cargo fmt --all -- --check` 通过。
- `cargo clippy --workspace --all-targets -- -D warnings` 通过。
- `cargo test --workspace` 通过，6 个测试成功。
- `client-ffi` 暴露 ABI 版本、协议主版本和特性位三个窄 C ABI 查询；macOS dylib 与 Android 三 ABI `.so` 构建成功。
- `protocol` 通过 `prost-build` 编译仓库内 Protobuf，并有编码/解码往返测试。
- 媒体、文件和安全 crate 已用最小领域类型替换生成器示例，但不代表对应业务功能已经实现。

## 6. Protobuf

协议源文件位于 `proto/crossdesktop/v1/`，当前包含 common、capability、device、input、session、signaling 六组基础消息。生成物位于各工程的 `build/generated`，不手工修改或提交。

```bash
./scripts/generate-proto.sh
```

该脚本依次执行 Java、Dart、Rust 生成/编译和 `buf lint`。当前已验证三种语言生成结果均可编译/分析；后续协议变更还必须增加 breaking check 和新旧版本互操作测试。

## 7. 本地基础设施

`infra/deploy/compose.dev.yaml` 提供版本锁定的 PostgreSQL、Redis 和 coturn，本地默认凭据仅用于开发环境。PostgreSQL/Redis 只监听 loopback，coturn 暴露 3478 TCP/UDP 和受限 UDP relay 端口段。

```bash
./scripts/dev-up.sh
docker compose -f infra/deploy/compose.dev.yaml ps
```

当前三个容器健康。`turnserver.dev.conf` 关闭本地 TLS/DTLS，仅用于开发验证；生产环境必须使用独立密钥、短期 TURN 凭据、TLS、网络访问控制和区域化部署。

## 8. 统一验收

```bash
CROSSDESKTOP_FLUTTER_BIN=/Volumes/zhiti-1T/Library/flutter/bin \
CROSSDESKTOP_ANDROID_NDK_HOME=/Volumes/zhiti-1T/Library/android-sdk/ndk/28.2.13676358 \
  ./scripts/check-all.sh
```

2026-08-17 会话保活与局域网发现优化后全流程通过：基础设施健康、Buf/三语言生成、Rust fmt/Clippy/6 项测试、Java 5 项测试、Flutter analyze/12 项测试，以及 macOS、iOS Simulator、Android Debug 构建。

## 9. 下一步顺序

1. 在物理 iPad 上验证 Bonjour 自动发现、点选填充、Mac 停止后设备消失。
2. 验证连接后切换设备/会话/设置页不断开，连接码和地址不清空。
3. 验证点击、拖动、滚动、坐标映射、拒绝、断开和重新连接。
4. 增加会话指标，优化到 1080p60 并完成 30 分钟稳定性测试。
5. 接入 TURN 短期凭据，验证 P2P/Relay 和网络切换。
6. Apple 纵向原型通过后，在 Windows 开发机启动 DXGI/硬编性能原型。
7. CI/CD 配置涉及仓库自动化策略，需用户明确批准后再增加。
